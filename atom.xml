<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>江湖十年</title>
  
  <subtitle>学而不思则罔，思而不学则殆。</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://www.jianghushinian.cn/"/>
  <updated>2019-10-28T13:31:00.839Z</updated>
  <id>http://www.jianghushinian.cn/</id>
  
  <author>
    <name>江湖十年</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Python 字典的一个奇异现象</title>
    <link href="http://www.jianghushinian.cn/2019/10/28/Python-%E5%AD%97%E5%85%B8%E7%9A%84%E4%B8%80%E4%B8%AA%E5%A5%87%E5%BC%82%E7%8E%B0%E8%B1%A1/"/>
    <id>http://www.jianghushinian.cn/2019/10/28/Python-字典的一个奇异现象/</id>
    <published>2019-10-28T13:15:12.000Z</published>
    <updated>2019-10-28T13:31:00.839Z</updated>
    
    <content type="html"><![CDATA[<p>字典（<code>dict</code>）类型在 Python 基础数据类型中有着举足轻重的地位，日常编码中几乎离不开字典的使用。不过字典中有一个看似非常奇怪的现象，常被人忽略。本篇文章就来探索这一奇异现象。</p><a id="more"></a><p>请用 5 秒钟思考一下，下面这个字典表达式会输出什么？</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>&#123;<span class="literal">True</span>: <span class="string">'True'</span>, <span class="number">1</span>: <span class="string">'1'</span>, <span class="number">1.0</span>: <span class="string">'1.0'</span>&#125;</span><br></pre></td></tr></table></figure><p>将以上字典表达式复制到 Python 控制台中执行，将会得到如下结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#123;True: &apos;1.0&apos;&#125;</span><br></pre></td></tr></table></figure><p>初次见到这个结果时我是非常吃惊的，what ？这看起来似乎并不符合常理，但当我们一步一步分析出背后的原因之后，就不会对这个结果感到奇怪了。</p><p>我们可以将上面的字典表达式拆解为多个步骤来分析。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>d = dict()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d[<span class="literal">True</span>] = <span class="string">'True'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d[<span class="number">1</span>] = <span class="string">'1'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d[<span class="number">1.0</span>] = <span class="string">'1.0'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d</span><br><span class="line">&#123;<span class="literal">True</span>: <span class="string">'1.0'</span>&#125;</span><br></pre></td></tr></table></figure><p>首先，先创建一个空的字典对象，然后依次给字典赋值，最终得到字典结果为 <code>{True: &#39;1.0&#39;}</code>。</p><p>从以上代码的执行步骤和结果，我们可以分析出，在 Python 中字典会将作为键的 <code>True</code>、<code>1</code> 和 <code>1.0</code> 认为是 <code>相等</code> 的。我们可以在 Python 控制台得到验证。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="literal">True</span> == <span class="number">1</span> == <span class="number">1.0</span></span><br><span class="line"><span class="literal">True</span></span><br></pre></td></tr></table></figure><p>果然，这三个对象对 python 来说是 <code>相等</code> 的。实际上，在 Python 中，<code>bool</code> 类型继承自 <code>int</code> 类型，所以这三个对象 <code>相等</code> 也是合理的。</p><p>Python 提供了 <code>issubclass</code> 函数可以判断一个类型是否是另一个类型的子类，使用这个函数来验证 <code>bool</code> 类型是 <code>int</code> 类型的子类。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>issubclass(bool, int)</span><br><span class="line"><span class="literal">True</span></span><br></pre></td></tr></table></figure><p>还可以使用 <code>isinstance</code> 函数来判断一个对象是否是一个类型的实例，由于 <code>True</code> 是 <code>bool</code> 类型的实例，那么 <code>True</code> 自然也是 <code>int</code> 类型的实例对象。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance(<span class="literal">True</span>, int)</span><br><span class="line"><span class="literal">True</span></span><br></pre></td></tr></table></figure><p>我们证实了 <code>True</code>、<code>1</code> 和 <code>1.0</code> 是 <code>相等</code> 的对象。但这还不足以说明问题，在 Python 中两个对象的值 <code>相等</code> 并不能说明它们放到字典中时会变为同一个键。这点通过稍后的示例就会明白。</p><p>在 Python 中，一个对象是否可以作为字典的键是有要求的，我们知道只有不可变类型才能作为字典的键。实际上 Python 判断一个对象是否可以作为字典的键，其实是判断这个对象是否为 <code>可哈希</code>（<code>hashable</code>）对象。</p><p>Python 在 <a href="https://docs.python.org/3/glossary.html#term-hashable" target="_blank" rel="noopener">官网文档</a> 中对 <code>可哈希</code> 对象进行了说明。大概意思是说：如果一个对象的 <code>hash</code> 值在其生命周期内不会改变（需要实现 <code>__hash__()</code> 方法），并且这个对象可以与其他对象进行比较（需要实现 <code>__eq__()</code> 方法），则该对象是 <code>可哈希</code> 的。由于 Python 内置的常见不可变类型都实现了 <code>__hash__()</code> 方法 和 <code>__eq__()</code> 方法，所以它们都是 <code>可哈希</code> 的。</p><p>这里还要强调一点：如果两个 <code>可哈希</code> 对象的值是相等的，那么它们的 <code>hash</code> 值也必然是相等的。这是 Python 的规范，我们自定义的类型也要遵循这个规范。</p><p>说的直白一些，Python 字典的键是不可重复的，而 Python 在操作字典的键时，如果两个对象的 <code>hash</code> 值相同，并且这两个对象的值也 <code>相等</code>，那么这两个对象会被当作同一个键。</p><p>Python 提供了 <code>hash</code> 函数可以获得一个对象的 <code>hash</code> 值，它会自动调用对象的 <code>__hash__()</code> 方法。至于判断两个对象的值是否相等，实际上就是使用 <code>==</code> 运算符，它会自动调用对象的 <code>__eq__()</code> 方法。</p><p>知道了有关字典的键的特性，接下来我们自己实现一个类，通过三个示例分别对三种不同的情况进行探索，从而通过实际的代码来验证字典的键的特性。</p><p>（以下示例代码中定义的 <code>__init__</code> 方法和 <code>__repr__</code> 方法只用于辅助观察，并不会对结果产生影响。）</p><p>示例一：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="class"><span class="keyword">class</span> <span class="title">A</span><span class="params">(object)</span>:</span></span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name)</span>:</span></span><br><span class="line"><span class="meta">... </span>            self.name = name</span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line"><span class="meta">... </span>            <span class="keyword">return</span> self.name</span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__eq__</span><span class="params">(self, other)</span>:</span></span><br><span class="line"><span class="meta">... </span>            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__hash__</span><span class="params">(self)</span>:</span></span><br><span class="line"><span class="meta">... </span>            <span class="keyword">return</span> id(self)</span><br><span class="line">...</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a1 = A(<span class="string">'a1'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a2 = A(<span class="string">'a2'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a1 == a2</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>hash(a1), hash(a2)</span><br><span class="line">(<span class="number">4332959632</span>, <span class="number">4332959248</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d = &#123;a1: <span class="number">1</span>, a2: <span class="number">2</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d</span><br><span class="line">&#123;a1: <span class="number">1</span>, a2: <span class="number">2</span>&#125;</span><br></pre></td></tr></table></figure><p>由示例一可知，<code>a1</code> 和 <code>a2</code> 两个对象的值相等，但 <code>hash</code> 值不同，最终得到的字典并不会将这两个对象看作同一个键。</p><p>示例二：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="class"><span class="keyword">class</span> <span class="title">A</span><span class="params">(object)</span>:</span></span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name)</span>:</span></span><br><span class="line"><span class="meta">... </span>            self.name = name</span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line"><span class="meta">... </span>            <span class="keyword">return</span> self.name</span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__eq__</span><span class="params">(self, other)</span>:</span></span><br><span class="line"><span class="meta">... </span>            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__hash__</span><span class="params">(self)</span>:</span></span><br><span class="line"><span class="meta">... </span>            <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line">...</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a1 = A(<span class="string">'a1'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a2 = A(<span class="string">'a2'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a1 == a2</span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>hash(a1), hash(a2)</span><br><span class="line">(<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d = &#123;a1: <span class="number">1</span>, a2: <span class="number">2</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d</span><br><span class="line">&#123;a1: <span class="number">1</span>, a2: <span class="number">2</span>&#125;</span><br></pre></td></tr></table></figure><p>由示例二可知，<code>a1</code> 和 <code>a2</code> 两个对象的 <code>hash</code> 值相同，但值不相等，最终得到的字典也不会将这两个对象看作同一个键。</p><p>示例三：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="class"><span class="keyword">class</span> <span class="title">A</span><span class="params">(object)</span>:</span></span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name)</span>:</span></span><br><span class="line"><span class="meta">... </span>            self.name = name</span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__repr__</span><span class="params">(self)</span>:</span></span><br><span class="line"><span class="meta">... </span>            <span class="keyword">return</span> self.name</span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__eq__</span><span class="params">(self, other)</span>:</span></span><br><span class="line"><span class="meta">... </span>            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"><span class="meta">... </span>    <span class="function"><span class="keyword">def</span> <span class="title">__hash__</span><span class="params">(self)</span>:</span></span><br><span class="line"><span class="meta">... </span>            <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line">...</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a1 = A(<span class="string">'a1'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a2 = A(<span class="string">'a2'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a1 == a2</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>hash(a1), hash(a2)</span><br><span class="line">(<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d = &#123;a1: <span class="number">1</span>, a2: <span class="number">2</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d</span><br><span class="line">&#123;a1: <span class="number">2</span>&#125;</span><br></pre></td></tr></table></figure><p>由示例三可知，<code>a1</code> 和 <code>a2</code> 两个对象的 <code>hash</code> 值相同，并且值也相等，最终得到的字典会将这两个对象看作同一个键。</p><p>通过以上三个示例代码的演示，想必不用我多说，你一定已经猜测到了，实际上在 Python 中，<code>True</code>、<code>1</code> 和 <code>1.0</code> 这三个对象的 <code>hash</code> 值也是相同的。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>hash(<span class="literal">True</span>), hash(<span class="number">1</span>), hash(<span class="number">1.0</span>)</span><br><span class="line">(<span class="number">1</span>, <span class="number">1</span>, <span class="number">1</span>)</span><br></pre></td></tr></table></figure><p>至此，关于 Python 字典中的 <code>奇异</code> 现象也就解释通了，下次再见到同样的问题就不会觉得奇怪了。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;字典（&lt;code&gt;dict&lt;/code&gt;）类型在 Python 基础数据类型中有着举足轻重的地位，日常编码中几乎离不开字典的使用。不过字典中有一个看似非常奇怪的现象，常被人忽略。本篇文章就来探索这一奇异现象。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>Python lambda 表达式</title>
    <link href="http://www.jianghushinian.cn/2019/10/27/Python-lambda-%E8%A1%A8%E8%BE%BE%E5%BC%8F/"/>
    <id>http://www.jianghushinian.cn/2019/10/27/Python-lambda-表达式/</id>
    <published>2019-10-27T10:40:42.000Z</published>
    <updated>2019-10-27T10:50:02.126Z</updated>
    
    <content type="html"><![CDATA[<p>我们知道在 Python 中定义一个函数使用 <code>def</code> 关键字。实际上，Python 中还有另一种定义函数的语法，使用 <code>lambda</code> 关键字即可定义一个短小的 <code>匿名函数</code>。</p><a id="more"></a><h3 id="lambad-表达式的定义语法"><a href="#lambad-表达式的定义语法" class="headerlink" title="lambad 表达式的定义语法"></a>lambad 表达式的定义语法</h3><p>下面是一个使用 <code>lambda</code> 关键字定义 <code>匿名函数</code> 的简单示例，用于对传进来的两个参数进行求和：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">lambda</span> x, y: x + y</span><br></pre></td></tr></table></figure><p>其中，冒号(<code>:</code>)左侧的 <code>x, y</code> 变量分别是 <code>匿名函数</code> 的两个参数，而冒号(<code>:</code>)右侧的表达式 <code>x + y</code> 将作为 <code>匿名函数</code> 的 <code>返回值</code> 进行返回（这个过程是自动的，无需显式的写 <code>return</code> 语句进行返回，写了反而会报错）。</p><p>那么该如何调用这个 <code>匿名函数</code> 呢？请看下面的代码：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>(<span class="keyword">lambda</span> x, y: x + y)(<span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line"><span class="number">5</span></span><br></pre></td></tr></table></figure><p>要想调用 <code>匿名函数</code>，只需要在 <code>匿名函数</code> 最外层给它加上一个括号，然后就能像调用普通函数一样的语法，使用 <code>(参数1, 参数2)</code> 的形式调用它了。 在 Python 控制台执行以上代码可以得到正确的求和结果 <code>5</code>。</p><p>以上的 <code>匿名函数</code> 的定义和执行过程，如果使用普通函数来完成，代码如下：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(x, y)</span>:</span></span><br><span class="line"><span class="meta">... </span>    <span class="keyword">return</span> x + y</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>add(<span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line"><span class="number">5</span></span><br></pre></td></tr></table></figure><p>经过对比，我们可以发现，使用 <code>def</code> 的普通函数需要 3 行才能搞定的事情，使用 <code>匿名函数</code> 只需要 1 行就可以了。</p><p>用 <code>lambda</code> 关键字定义的 <code>匿名函数</code> 只是一个 Python 的语法糖，它与 <code>def</code> 关键字定义的函数并无本质区别。不过需要注意的是，在 <code>匿名函数</code> 中，只允许定义单一的表达式，而不能定义其他语句，并且表达式的结果会被当作函数的返回值进行返回，因此 <code>匿名函数</code> 也被称作 <code>lambda 表达式</code>。</p><p>在 Python 中函数也是对象，因此我们可以将函数赋值给一个变量，然后通过这个变量来调用函数。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>add = <span class="keyword">lambda</span> x, y: x + y</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>add(<span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line"><span class="number">5</span></span><br></pre></td></tr></table></figure><p>事实上，这种通过变量引用来调用 <code>匿名函数</code> 的方式才是我们在实际编码过程中用的更多的方式。</p><h3 id="lambda-表达式的应用"><a href="#lambda-表达式的应用" class="headerlink" title="lambda 表达式的应用"></a>lambda 表达式的应用</h3><p>下面我来介绍一下 <code>匿名函数</code> 的应用场景。</p><p>假如我们有一个由 <code>元组</code> 组成的 <code>列表</code>，我们需要对其进行排序，此时可以使用 Python 内置的 <code>sorted</code> 函数：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>li = [(<span class="number">1</span>, <span class="string">'Python'</span>), (<span class="number">3</span>, <span class="string">'JavaScript'</span>), (<span class="number">2</span>, <span class="string">'Golang'</span>)]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>sorted(li)</span><br><span class="line">[(<span class="number">1</span>, <span class="string">'Python'</span>), (<span class="number">2</span>, <span class="string">'Golang'</span>), (<span class="number">3</span>, <span class="string">'JavaScript'</span>)]</span><br></pre></td></tr></table></figure><p>可以看到，对 <code>列表</code> 使用 <code>sorted</code> 函数对其进行排序以后，其排序顺序是根据每个元组中第一个元素的大小进行排序的。那么如果我们想要根据元组的第二个元素按照字母顺序进行排序呢？这个时候，就可以用到 <code>匿名函数</code> 了。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>li = [(<span class="number">1</span>, <span class="string">'Python'</span>), (<span class="number">3</span>, <span class="string">'JavaScript'</span>), (<span class="number">2</span>, <span class="string">'Golang'</span>)]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>sorted(li, key=<span class="keyword">lambda</span> x: x[<span class="number">1</span>])</span><br><span class="line">[(<span class="number">2</span>, <span class="string">'Golang'</span>), (<span class="number">3</span>, <span class="string">'JavaScript'</span>), (<span class="number">1</span>, <span class="string">'Python'</span>)]</span><br><span class="line">&gt;&gt;&gt;</span><br></pre></td></tr></table></figure><p><code>sorted</code> 函数接收一个参数 <code>key</code> 可以用来作为排序对象的排序依据，这个参数必须是一个可调用对象，所以通常我们可以定义一个函数来处理。但是一般来说这样的函数只会使用一次，并且这个示例所需的函数足够短小，此时正是使用 <code>匿名函数</code> 的最佳时机。</p><p>同 <code>def</code> 关键字定义的函数一样，使用 <code>lambda</code> 关键字定义的 <code>匿名函数</code> 同样支持嵌套。下面展示一个 <code>lambda 表达式</code> 版本的 <code>闭包函数</code>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>make_add = <span class="keyword">lambda</span> x: <span class="keyword">lambda</span> y: x + y</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>add = make_add(<span class="number">2</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>add(<span class="number">3</span>)</span><br><span class="line"><span class="number">5</span></span><br></pre></td></tr></table></figure><p>我在此必须强调一点，这种嵌套的写法虽然语法上是支持的，但在实际工作中千万不要这样来写，如果项目中充斥着大量的这种代码，那么这个项目将变得难以维护。给出这个示例，我只是想鼓励大家对于一个新接触的语法可以多进行尝试，这样才能进步得更快，同时获得更多的乐趣。</p><p>在 Python 中对于 <code>匿名函数</code> 的使用，我的观点是，只有需要一个非常短小且只在一处调用的函数时才使用，不然代码将会变得花里胡哨而难以维护。</p><p><code>匿名函数</code> 的诸多限制至使它只能完成一个简单的工作，不过这也正是 <code>匿名函数</code> 的初衷，至于许多繁琐的工作，还是交给 <code>def</code> 去做吧。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;我们知道在 Python 中定义一个函数使用 &lt;code&gt;def&lt;/code&gt; 关键字。实际上，Python 中还有另一种定义函数的语法，使用 &lt;code&gt;lambda&lt;/code&gt; 关键字即可定义一个短小的 &lt;code&gt;匿名函数&lt;/code&gt;。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>Python 闭包</title>
    <link href="http://www.jianghushinian.cn/2019/10/21/Python-%E9%97%AD%E5%8C%85/"/>
    <id>http://www.jianghushinian.cn/2019/10/21/Python-闭包/</id>
    <published>2019-10-21T12:32:23.000Z</published>
    <updated>2019-10-22T12:27:51.620Z</updated>
    
    <content type="html"><![CDATA[<p><code>闭包</code>（closure）作为一个不太容易理解的概念出现在很多主流编程语言中，Python 中很多高级实现都离不开 <code>闭包</code>，<code>装饰器</code> 就是使用 <code>闭包</code> 的典型例子。</p><a id="more"></a><h3 id="作用域"><a href="#作用域" class="headerlink" title="作用域"></a>作用域</h3><p>要学习 <code>闭包</code>，先了解 <code>作用域</code> 的概念。</p><p><code>作用域</code> 是程序运行时变量的存在范围。常见 <code>作用域</code> 有 <code>全局作用域</code> 和 <code>局部作用域</code>，定义在<code>全局作用域</code>中的变量，在程序运行过程中的任何地方都可以访问到；而定义在函数内部的变量只有在函数内部才能够访问，函数内部的作用域就是<code>局部作用域</code>，为了便于理解，我这里称它为<code>函数作用域</code>。</p><p><code>全局作用域</code>不可以读取 <code>函数作用域</code> 中的局部变量：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">()</span>:</span></span><br><span class="line">    num = <span class="number">100</span></span><br><span class="line"></span><br><span class="line">print(num)  <span class="comment"># NameError: name 'num' is not defined</span></span><br></pre></td></tr></table></figure><p><code>函数作用域</code> 可以向上读取 <code>全局作用域</code> 中的全局变量：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">num = <span class="number">100</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">()</span>:</span></span><br><span class="line">    print(num)  <span class="comment"># 100</span></span><br><span class="line"></span><br><span class="line">foo()</span><br></pre></td></tr></table></figure><h3 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h3><p>在 Python 中，函数是 <code>一等公民</code>，意思是说你可以像看待 <code>int</code>、<code>dict</code> 等常见的数据类型一样去看待它。其实函数不仅仅可以定义在 <code>全局作用域</code> 中，函数也可以定义在另一个 <code>函数作用域</code> 中。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">()</span>:</span></span><br><span class="line">    num = <span class="number">100</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">bar</span><span class="params">()</span>:</span></span><br><span class="line">        print(num)  <span class="comment"># 100</span></span><br><span class="line"></span><br><span class="line">    bar()</span><br><span class="line"></span><br><span class="line">foo()</span><br></pre></td></tr></table></figure><p>以上代码中，在 <code>foo</code> 函数内部又定义了一个 <code>bar</code> 函数。<code>bar</code> 函数可以向上读取 <code>foo</code> 函数内定义的任何变量。<code>bar</code> 函数读取变量 <code>num</code> 的顺序是：先在 <code>bar</code> 函数自己的 <code>函数作用域</code> 内查找，如果找不到就向上查找 <code>foo</code> 函数的 <code>作用域</code>，如果还是找不到，最后再向上查找 <code>全局作用域</code>。</p><p>以上代码稍作修改，就能成为一个 <code>闭包</code> 函数：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">()</span>:</span></span><br><span class="line">    num = <span class="number">100</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">bar</span><span class="params">()</span>:</span></span><br><span class="line">        print(num)  <span class="comment"># 100</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> bar</span><br><span class="line"></span><br><span class="line">result = foo()</span><br><span class="line">result()</span><br></pre></td></tr></table></figure><p>可以看到，只需要将之前的示例代码中 <code>bar()</code> 这句函数调用，改为 <code>return bar</code>，然后在调用 <code>foo</code> 函数时用一个变量 <code>result</code> 接收 <code>foo</code> 函数的返回值，最后再调用 <code>result</code> 这个变量，就能得到同样的结果。这样其实间接的的实现了在 <code>全局作用域</code> 中 <code>读取函数作用域</code> 中的变量。这就是 <code>闭包</code> 的威力。</p><p>像这样，函数嵌套函数，内部函数引用外部函数 <code>作用域</code> 中的局部变量，外部函数将内部函数的引用当作返回值返回，此时，我们就可以将内部函数和它引用的外部函数 <code>作用域</code> 中的局部变量统称为 <code>闭包</code>。</p><h3 id="闭包的应用"><a href="#闭包的应用" class="headerlink" title="闭包的应用"></a>闭包的应用</h3><p>假如需要实现一个计数器，我们很容易想到使用 <code>类</code> 来实现：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Counter</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.num = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__call__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.num += <span class="number">1</span></span><br><span class="line">        <span class="keyword">return</span> self.num</span><br><span class="line"></span><br><span class="line">counter = Counter()</span><br><span class="line">print(counter())  <span class="comment"># 1</span></span><br><span class="line">print(counter())  <span class="comment"># 2</span></span><br><span class="line">print(counter())  <span class="comment"># 3</span></span><br></pre></td></tr></table></figure><p>由于 <code>类</code> 可以保存数据并且操作数据，所以很轻松就能够使用 <code>类</code> 来实现计数器。</p><p>而函数本身没法在每次调用时保存数据，所以无法实现一个计数器的功能。但当我们有了 <code>闭包</code> 函数，就能够用 <code>函数</code> 的形式来实现计数器了。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">make_counter</span><span class="params">()</span>:</span></span><br><span class="line">    num = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">counter</span><span class="params">()</span>:</span></span><br><span class="line">        <span class="keyword">nonlocal</span> num</span><br><span class="line">        num += <span class="number">1</span></span><br><span class="line">        <span class="keyword">return</span> num</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> counter</span><br><span class="line"></span><br><span class="line">counter = make_counter()</span><br><span class="line">print(counter())  <span class="comment"># 1</span></span><br><span class="line">print(counter())  <span class="comment"># 2</span></span><br><span class="line">print(counter())  <span class="comment"># 3</span></span><br></pre></td></tr></table></figure><p>以上，我们就用 <code>闭包函数</code> 实现了一个计数器。</p><p>可以看到内部 <code>counter</code> 函数有一条语句 <code>nonlocal num</code>，关键字 <code>nonlocal</code> 的作用可以对应 <code>global</code> 关键字来理解。当我们在 <code>函数作用域</code> 内部修改 <code>全局作用域</code> 中的 <code>不可变类型</code> 变量时，我们会使用 <code>global</code> 关键字来标明一个变量是全局变量，同样的 <code>nonlocal</code> 关键字的作用是标明 <code>num</code> 是一个 <code>闭包</code> 中的变量，它有一个专业的术语叫做 <code>自由变量</code>。通常来说，一个函数执行完成以后，其内部的变量会随之被销毁，而 <code>自由变量</code> <code>num</code> 并不会被立即销毁，它随同 <code>counter</code> 函数一起组成了 <code>闭包</code>。</p><h3 id="闭包的陷阱"><a href="#闭包的陷阱" class="headerlink" title="闭包的陷阱"></a>闭包的陷阱</h3><p>下面是一个使用 <code>闭包</code> 时常见的误区：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">func</span><span class="params">()</span>:</span></span><br><span class="line">    li = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">3</span>):</span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">()</span>:</span></span><br><span class="line">            <span class="keyword">return</span> i</span><br><span class="line"></span><br><span class="line">        li.append(f)</span><br><span class="line">    <span class="keyword">return</span> li</span><br><span class="line"></span><br><span class="line">li = func()</span><br><span class="line"><span class="keyword">for</span> f <span class="keyword">in</span> li:</span><br><span class="line">    print(f())</span><br><span class="line">   </span><br><span class="line"><span class="comment"># 运行结果</span></span><br><span class="line"><span class="comment"># 2</span></span><br><span class="line"><span class="comment"># 2</span></span><br><span class="line"><span class="comment"># 2</span></span><br></pre></td></tr></table></figure><p>我们期待的打印结果依次为 <code>0</code>、<code>1</code>、<code>2</code>，但实际上运行以上代码，得到的结果确是 <code>2</code>、<code>2</code>、<code>2</code>。</p><p>这是因为，内部函数 <code>f</code> 和循环变量 <code>i</code> 形成了 <code>闭包</code>。<code>func</code> 函数内部共产生了 3 个 <code>f</code> 函数，它们依次被追加到 <code>li</code> 列表中，但是并没有立即被执行，而其内部引用了 <code>自由变量</code> <code>i</code>，在 3 次 <code>for</code> 循环执行完成后，<code>i</code> 的值已经变成了 <code>2</code>，等到在函数外部接收到 <code>li</code> 再一次循环执行每个 <code>f</code> 函数时，所打印的 <code>i</code> 的值自然就都为 <code>2</code> 了。</p><p>要解决这个问题，需要想办法在每次循环时，让内部的 <code>闭包</code> 函数 <code>f</code> 绑定住当前的循环变量，而不使其跟随 <code>i</code> 的变动而改变：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">func</span><span class="params">()</span>:</span></span><br><span class="line">    li = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">3</span>):</span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">wrap</span><span class="params">(j)</span>:</span></span><br><span class="line">            <span class="function"><span class="keyword">def</span> <span class="title">f</span><span class="params">()</span>:</span></span><br><span class="line">                <span class="keyword">return</span> j</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> f</span><br><span class="line"></span><br><span class="line">        li.append(wrap(i))</span><br><span class="line">    <span class="keyword">return</span> li</span><br><span class="line"></span><br><span class="line">li = func()</span><br><span class="line"><span class="keyword">for</span> f <span class="keyword">in</span> li:</span><br><span class="line">    print(f())</span><br><span class="line"></span><br><span class="line"><span class="comment"># 运行结果</span></span><br><span class="line"><span class="comment"># 0</span></span><br><span class="line"><span class="comment"># 1</span></span><br><span class="line"><span class="comment"># 2</span></span><br></pre></td></tr></table></figure><p>在这里，我在内部函数 <code>f</code> 的外部又嵌套了一层函数 <code>wrap</code>，在执行 <code>li.append(wrap(i))</code> 语句时，将循环变量 <code>i</code> 的值传递给 <code>wrap</code> 函数，这样 <code>wrap</code> 函数执行完成后，函数 <code>f</code> 和变量 <code>j</code> 就组成了 <code>闭包</code>。每次 <code>for</code> 循环时，都将循环变量 <code>i</code> 当作参数传递给 <code>wrap</code> 函数，由于每产生的 <code>wrap</code> 函数相互独立并且绑定到函数参数上的变量值不会改变，这样就能保证每次循环过程中在 <code>wrap</code> 函数内部保存的变量 <code>j</code> 互相独立，所以最终能够得到预期的结果。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;code&gt;闭包&lt;/code&gt;（closure）作为一个不太容易理解的概念出现在很多主流编程语言中，Python 中很多高级实现都离不开 &lt;code&gt;闭包&lt;/code&gt;，&lt;code&gt;装饰器&lt;/code&gt; 就是使用 &lt;code&gt;闭包&lt;/code&gt; 的典型例子。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>Python 运算符 + 与 +=</title>
    <link href="http://www.jianghushinian.cn/2019/10/13/Python-%E8%BF%90%E7%AE%97%E7%AC%A6-%E4%B8%8E/"/>
    <id>http://www.jianghushinian.cn/2019/10/13/Python-运算符-与/</id>
    <published>2019-10-13T09:45:23.000Z</published>
    <updated>2019-10-13T11:05:18.267Z</updated>
    
    <content type="html"><![CDATA[<p>Python 中如果需要对一个变量进行增量运算通常有两种写法，<code>a = a + b</code> 或 <code>a += b</code>。虽然两种方法能够得到同样的结果，但两者却并不完全等价。</p><a id="more"></a><h3 id="加法运算符"><a href="#加法运算符" class="headerlink" title="加法运算符 +"></a>加法运算符 <code>+</code></h3><p>对于 <code>a = a + b</code> 这条语句来说，实际上执行了两步操作，首先执行 <code>a + b</code> 求出两者的 <code>和</code>，然后再将结果通过赋值运算符 <code>=</code> 赋值给变量 <code>a</code>。</p><p>来看下面的示例：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">100</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">200</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">1796532544</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = a + b</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line"><span class="number">300</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">17756816</span></span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">11</span>, <span class="number">22</span>, <span class="number">33</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = [<span class="number">44</span>, <span class="number">55</span>, <span class="number">66</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">54675856</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = a + b</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">11</span>, <span class="number">22</span>, <span class="number">33</span>, <span class="number">44</span>, <span class="number">55</span>, <span class="number">66</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">54676416</span></span><br></pre></td></tr></table></figure><p>以上分别使用 Python 中的 <code>不可变类型</code> 和 <code>可变类型</code> 各列举了一个示例，并且在执行 <code>a = a + b</code> 语句的前后分别打印了变量 <code>a</code> 的 <code>id</code>，可以看到无论对于 <code>不可变类型</code> 还是 <code>可变类型</code>，最终变量 <code>a</code> 的 <code>id</code> 值都会改变，说明变量 <code>a</code> 在执行 <code>a = a + b</code> 以后指向了一片新的内存地址。这也比较好理解，因为 <code>a</code> 已经被重新赋值了。</p><h3 id="增量赋值运算符"><a href="#增量赋值运算符" class="headerlink" title="增量赋值运算符 +="></a>增量赋值运算符 <code>+=</code></h3><p>对于运算符 <code>+=</code>，我们通常管它叫作 <code>增量赋值运算符</code>，因为它即完成了 <code>相加</code> 操作，又完成了 <code>赋值</code> 操作。</p><p>同样，我们还是分别用 <code>int</code> 和 <code>list</code> 两种数据类型来做演示：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">100</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">200</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">1796532544</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a += b</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line"><span class="number">300</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">17756784</span></span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">11</span>, <span class="number">22</span>, <span class="number">33</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = [<span class="number">44</span>, <span class="number">55</span>, <span class="number">66</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">48777616</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a += b</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">11</span>, <span class="number">22</span>, <span class="number">33</span>, <span class="number">44</span>, <span class="number">55</span>, <span class="number">66</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">48777616</span></span><br></pre></td></tr></table></figure><p>对于 <code>不可变类型</code> 的操作，<code>a += b</code> 表现出来的结果和 <code>a = a + b</code> 相同。而对于 <code>可变类型</code> 的操作却并不完全一样。虽然最终变量 <code>a</code> 的 <code>值</code> 相同，但执行 <code>+=</code> 操作后，变量 <code>a</code> 的内存地址并没有改变，也就是说 <code>+=</code> 操作对于 <code>可变类型</code> 来说实际上是 <code>就地更改</code>。对于 <code>list</code> 的操作，实际上 <code>a += b</code> 等价于 <code>a.extend(b)</code>。</p><h3 id="add-和-iadd-方法"><a href="#add-和-iadd-方法" class="headerlink" title="__add__ 和 __iadd__ 方法"></a><code>__add__</code> 和 <code>__iadd__</code> 方法</h3><p>实际上，在 Python 中，加法运算符 <code>+</code> 对应着对象的 <code>__add__</code> 方法，增量赋值运算符 <code>+=</code> 对应着对象的 <code>__iadd__</code> 方法。</p><p>无论对于 <code>不可变类型</code> 还是 <code>可变类型</code>，当执行 <code>a = a + b</code> 时，都会调用 <code>a</code> 的 <code>__add__</code> 方法。而对于 <code>a += b</code> 的操作来说，当 <code>a</code> 为 <code>不可变类型</code> 时同样会调用 <code>a</code> 的 <code>__add__</code> 方法，当 <code>a</code> 为 <code>可变类型</code> 时会调用 <code>a</code> 的 <code>__iadd__</code> 方法进行 <code>就地更改</code>，如果 <code>a</code> 没有实现 <code>__iadd__</code> 方法，那么才调用 <code>a</code> 的 <code>__add__</code> 方法。</p><p>我们可以用 Python 内置的 <code>hasattr</code> 函数来验证上面的说法。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>hasattr(int, <span class="string">'__add__'</span>)</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>hasattr(int, <span class="string">'__iadd__'</span>)</span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>hasattr(list, <span class="string">'__add__'</span>)</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>hasattr(list, <span class="string">'__iadd__'</span>)</span><br><span class="line"><span class="literal">True</span></span><br></pre></td></tr></table></figure><p>对于 <code>不可变类型</code> 来说，因为对象本身不可变，如果做相加操作必然会创建新的对象，所以也就没有 <code>__iadd__</code> 方法。而对于 <code>可变类型</code> 来说，对象本身可变，所以实现了 <code>__iadd__</code> 方法。</p><p>在我们自己定义的类型中如果需要实现以上两个方法，也要遵循 Python 现有的规范，一定要注意自己实现的类型是否可变，根据类型再来确定是否需要实现 <code>__iadd__</code> 方法。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Python 中如果需要对一个变量进行增量运算通常有两种写法，&lt;code&gt;a = a + b&lt;/code&gt; 或 &lt;code&gt;a += b&lt;/code&gt;。虽然两种方法能够得到同样的结果，但两者却并不完全等价。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>善用 setdefault 和 defaultdict 处理 Python 字典中找不到的键</title>
    <link href="http://www.jianghushinian.cn/2019/09/22/%E5%96%84%E7%94%A8-setdefault-%E5%92%8C-defaultdict-%E5%A4%84%E7%90%86-Python-%E5%AD%97%E5%85%B8%E4%B8%AD%E6%89%BE%E4%B8%8D%E5%88%B0%E7%9A%84%E9%94%AE/"/>
    <id>http://www.jianghushinian.cn/2019/09/22/善用-setdefault-和-defaultdict-处理-Python-字典中找不到的键/</id>
    <published>2019-09-22T07:04:37.000Z</published>
    <updated>2019-09-22T08:49:28.913Z</updated>
    
    <content type="html"><![CDATA[<p>当以 <code>dict[key]</code> 的形式获取字典中某个键的值的时候，如果 <code>key</code> 不在字典中，我们将得到一个 <code>KeyError</code> 异常。由于处理异常需要写额外的代码，所以我们往往更多的使用 <code>dict.get(key, defalut)</code> 的形式来获取字典中某个键的值。这种情况下如果 <code>key</code> 不在这个 <code>dict</code> 中，也会返回一个指定的默认值 <code>default</code>，而不至于抛出异常。</p><a id="more"></a><h3 id="使用-setdefault"><a href="#使用-setdefault" class="headerlink" title="使用 setdefault"></a>使用 <code>setdefault</code></h3><p>在实际工作中，我们从数据库中查询的数据有可能是下面这种形式。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">data = [(<span class="string">'one'</span>, <span class="number">1</span>), (<span class="string">'one'</span>, <span class="number">2</span>), (<span class="string">'two'</span>, <span class="number">1</span>), (<span class="string">'two'</span>, <span class="number">2</span>,), (<span class="string">'three'</span>, <span class="number">1</span>)]</span><br></pre></td></tr></table></figure><p>现在需要将其转换为字典，并根据列表中每个元组的第一个元素当作键来进行分组，将所有键相同的值存放到列表中，结果如下。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">d = &#123;</span><br><span class="line">    <span class="string">'one'</span>: [<span class="number">1</span>, <span class="number">2</span>],</span><br><span class="line">    <span class="string">'two'</span>: [<span class="number">1</span>, <span class="number">2</span>],</span><br><span class="line">    <span class="string">'three'</span>: [<span class="number">1</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>将列表转换为字典的过程，我们有可能写出这样的代码：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">d = &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> key, value <span class="keyword">in</span> data:</span><br><span class="line">    value_list = d.get(key, [])  <span class="comment"># 如果 key 不存在，返回一个空列表</span></span><br><span class="line">    value_list.append(value)</span><br><span class="line">    d[key] = value_list</span><br></pre></td></tr></table></figure><p>这段代码逻辑比较简单，在 <code>for</code> 循环中，每次获取到对应的 <code>key</code> 的值，如果不存在就初始化一个空列表，然后将新的值追加到列表中，最后重新赋值给字典对应的 <code>key</code>。</p><p>这样虽然能够实现功能，但在 Python 中实现它我们还有更好的办法，即使用字典的 <code>setdefault</code> 方法为不存在的 <code>key</code> 赋予默认值。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">d = &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> key, value <span class="keyword">in</span> data:</span><br><span class="line">    d.setdefault(key, []).append(value)</span><br></pre></td></tr></table></figure><p>如你所见，之前用三行代码才能解决的问题，使用 <code>setdefault</code> 后只需要一行代码即可搞定。</p><p>事实上，使用 <code>setdefault</code> 不止在于代码更加的简洁，它还能提高程序执行效率。当调用 <code>d.setdefault(key, [])</code> 时，如果在 <code>d</code> 中查询不到 <code>key</code>，<code>setdefault</code> 会直接创建一个空列表并赋值给 <code>d[key]</code>，最后返回这个新创建的空列表，所以能够在后续直接调用列表的 <code>append</code> 方法对其进行更新操作。这个过程只会在 <code>d</code> 中查询一次 <code>key</code> 这个键，而之前的做法中则需要查询两次。</p><h3 id="使用-defaultdict"><a href="#使用-defaultdict" class="headerlink" title="使用 defaultdict"></a>使用 <code>defaultdict</code></h3><p><code>defaultdict</code> 是 Python 标准库 <code>collections</code> 下提供的一个能够以 <code>Pythonic</code> 的方式解决上面问题的另一种实现方案。</p><p><code>defaultdict</code> 跟普通的 <code>dict</code> 用法几乎一样，只不过需要在其构造方法中提供一个可调用对象作为参数。当在字典中查询某个 <code>key</code> 不存在时，这个可调用对象的返回结果即为这个 <code>key</code> 的默认值。</p><p>以下是使用 <code>defaultdict</code> 来解决上面问题的代码示例：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> defaultdict</span><br><span class="line"></span><br><span class="line">d = defaultdict(list)  <span class="comment"># 用 list 的构造方法来创建默认值</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> key, value <span class="keyword">in</span> data:</span><br><span class="line">    d[key].append(value)</span><br></pre></td></tr></table></figure><p>注意：在实例化 <code>defaultdict</code> 时，如果没有传入参数，字典仍能够被正常创建，但当遇到不存在的 <code>key</code> 时就会抛出 <code>KeyError</code> 异常。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;当以 &lt;code&gt;dict[key]&lt;/code&gt; 的形式获取字典中某个键的值的时候，如果 &lt;code&gt;key&lt;/code&gt; 不在字典中，我们将得到一个 &lt;code&gt;KeyError&lt;/code&gt; 异常。由于处理异常需要写额外的代码，所以我们往往更多的使用 &lt;code&gt;dict.get(key, defalut)&lt;/code&gt; 的形式来获取字典中某个键的值。这种情况下如果 &lt;code&gt;key&lt;/code&gt; 不在这个 &lt;code&gt;dict&lt;/code&gt; 中，也会返回一个指定的默认值 &lt;code&gt;default&lt;/code&gt;，而不至于抛出异常。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
      <category term="Pythonic" scheme="http://www.jianghushinian.cn/tags/Pythonic/"/>
    
  </entry>
  
  <entry>
    <title>Python 上下文管理器实现</title>
    <link href="http://www.jianghushinian.cn/2019/09/18/Python-%E4%B8%8A%E4%B8%8B%E6%96%87%E7%AE%A1%E7%90%86%E5%99%A8%E5%AE%9E%E7%8E%B0/"/>
    <id>http://www.jianghushinian.cn/2019/09/18/Python-上下文管理器实现/</id>
    <published>2019-09-18T14:33:35.000Z</published>
    <updated>2019-09-18T14:56:33.314Z</updated>
    
    <content type="html"><![CDATA[<p>在 Web 开发中，我们经常需要对资源进行操作，如从数据库中读取数据、日志文件的写入等。这些都是常见的资源操作，而资源是有限的，所以当我们对资源操作完成以后就需要对其进行释放。如果资源没有得到释放，当资源占用数达到了操作系统的限定数，就会导致程序崩溃。</p><a id="more"></a><p>在文件操作过程中，为了保证文件操作完成后关闭文件资源，我们可能会写出这样的代码：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    f = open(<span class="string">'demo.txt'</span>, <span class="string">'w'</span>)</span><br><span class="line">    f.write(<span class="string">'hello'</span>)</span><br><span class="line"><span class="keyword">finally</span>:</span><br><span class="line">    f.close()</span><br></pre></td></tr></table></figure><p>这是一个典型的资源操作的例子，不过 Python 提供了 <code>with</code> 语句可以对以上代码进行简化：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">with</span> open(<span class="string">'demo.txt'</span>, <span class="string">'w'</span>) <span class="keyword">as</span> f:</span><br><span class="line">    f.write(<span class="string">'hello'</span>)</span><br></pre></td></tr></table></figure><p>可以看到，用 <code>with</code> 重写过的代码中并没有显式的调用 <code>f.close()</code> 方法来关闭文件，但当 <code>with</code> 代码块执行完以后，文件还是会被正常关闭的。即使在调用 <code>f.write()</code> 时产生异常，文件同样会被关闭。</p><p>这就是 <code>with</code> 语句的强大之处，它不仅可以简化我们的代码，并且能够自动释放资源。其实这就是所谓的 <code>上下文管理器</code>。能够自动分配和释放资源正是上下文管理器最强大的地方。</p><p><code>open</code> 函数能够通过 <code>with</code> 语句完成自动化的上下文管理，实际上，我们同样可以实现自定义的 <code>上下文管理器</code>。Python 为我们实现自定义 <code>上下文管理器</code> 提供了一个统一的接口——<code>上下文管理协议</code>。只要我们按照 Python 的规定实现了 <code>上下文管理协议</code>，就能够实现自定义的 <code>上下文管理器</code>。</p><h3 id="基于类实现上下文管理器"><a href="#基于类实现上下文管理器" class="headerlink" title="基于类实现上下文管理器"></a>基于类实现上下文管理器</h3><p>我们知道，在 Python 中有很多双下划线开头、双下划线结尾的方法，我们通常称作 <code>魔法方法</code>。基于类的上下文管理器就是通过 <code>__enter__</code>、<code>__exit__</code> 两个 <code>魔法方法</code> 来实现的。</p><p>下面通过一个自定义的文件上下文管理器类来介绍上下文管理器的实现：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyFileManager</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, filename, mode)</span>:</span></span><br><span class="line">        print(<span class="string">'call __init__'</span>)</span><br><span class="line">        self.filename = filename</span><br><span class="line">        self.mode = mode</span><br><span class="line">        self.file = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__enter__</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">'call __enter__'</span>)</span><br><span class="line">        self.file = open(self.filename, self.mode)</span><br><span class="line">        <span class="keyword">return</span> self.file</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__exit__</span><span class="params">(self, exc_type, exc_val, exc_tb)</span>:</span></span><br><span class="line">        print(<span class="string">'call __exit__'</span>)</span><br><span class="line">        <span class="keyword">if</span> self.file:</span><br><span class="line">            self.file.close()</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> MyFileManager(<span class="string">'demo.txt'</span>, <span class="string">'w'</span>) <span class="keyword">as</span> f:</span><br><span class="line">    print(<span class="string">'write file'</span>)</span><br><span class="line">    f.write(<span class="string">'test'</span>)</span><br></pre></td></tr></table></figure><p>代码执行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">call __init__</span><br><span class="line">call __enter__</span><br><span class="line">write file</span><br><span class="line">call __exit__</span><br></pre></td></tr></table></figure><p>可以看到，只需要十几行的代码，我们就实现了一个上下文管理器。而它的效果和 <code>with open</code> 并无两样。由代码执行结果我们可以分析出代码的执行顺序。首先在 <code>with</code> 语句后面实例化 <code>MyFileManager</code> 类，实例化过程中会调用类的 <code>__init__</code> 方法，接着自动调用了 <code>__enter__</code> 方法并拿到返回值，即文件对象，<code>as f</code> 就是将 <code>__enter__</code> 方法返回的文件对象赋值给 <code>f</code> 变量，然后执行 <code>with</code> 代码块中的代码对文件进行写入，当程序将要退出 <code>with</code> 代码块时，就会自动调用 <code>MyFileManager</code>  类的 <code>__exit__</code> 方法。</p><p>以上就是自定义 <code>上下文管理器</code> 的执行逻辑。要实现 <code>上下文管理器</code>，<code>__enter__</code>、<code>__exit__</code> 两个方法是必须实现的。其中 <code>__enter__</code> 方法用来获取资源，所以它将在 <code>with</code> 语句代码块开始执行之前被调用，<code>__exit__</code> 方法用来释放资源，它将在 <code>with</code> 语句代码块执行结束将要退出时被调用。</p><p>需要额外注意的是 <code>__exit__</code> 方法的参数，相比 <code>__enter__</code> 方法它多了三个参数，这三个参数的作用分别是：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">exc_type: 异常类型</span><br><span class="line">exc_val: 异常的值</span><br><span class="line">exc_tb:异常栈</span><br></pre></td></tr></table></figure><p>当执行 <code>with</code> 语句块中的代码，程序有可能会抛出异常，而 <code>__exit__</code> 方法的这三个参数，就会包含 <code>with</code> 语句块中产生的异常信息。所以 <code>__exit__</code> 方法还为我们提供了另外一个功能，即可以在此方法内部对异常进行处理，由开发者来决定异常的处理方式。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyFileManager</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, filename, mode)</span>:</span></span><br><span class="line">        print(<span class="string">'call __init__'</span>)</span><br><span class="line">        self.filename = filename</span><br><span class="line">        self.mode = mode</span><br><span class="line">        self.file = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__enter__</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">'call __enter__'</span>)</span><br><span class="line">        self.file = open(self.filename, self.mode)</span><br><span class="line">        <span class="keyword">return</span> self.file</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__exit__</span><span class="params">(self, exc_type, exc_val, exc_tb)</span>:</span></span><br><span class="line">        print(<span class="string">'call __exit__'</span>)</span><br><span class="line">        <span class="keyword">if</span> self.file:</span><br><span class="line">            self.file.close()</span><br><span class="line">        <span class="keyword">if</span> exc_type:</span><br><span class="line">            print(<span class="string">f'exc_type: <span class="subst">&#123;exc_type&#125;</span>'</span>)</span><br><span class="line">            print(<span class="string">f'exc_val: <span class="subst">&#123;exc_val&#125;</span>'</span>)</span><br><span class="line">            print(<span class="string">f'exc_tb: <span class="subst">&#123;exc_tb&#125;</span>'</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> MyFileManager(<span class="string">'demo.txt'</span>, <span class="string">'w'</span>) <span class="keyword">as</span> f:</span><br><span class="line">    <span class="keyword">raise</span> Exception(<span class="string">'write error'</span>)</span><br></pre></td></tr></table></figure><p>代码执行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">call __init__</span><br><span class="line">call __enter__</span><br><span class="line">call __exit__</span><br><span class="line">exc_type: &lt;class &apos;Exception&apos;&gt;</span><br><span class="line">exc_val: write error</span><br><span class="line">exc_tb: &lt;traceback object at 0x036D08A0&gt;</span><br></pre></td></tr></table></figure><p>可以看到，在 <code>with</code> 代码块中抛出的异常已经被 <code>__exit__</code> 方法捕获了。需要注意的是，在 <code>__exit__</code> 方法内部的最后还返回了 <code>True</code>，这代表异常已经处理，而不需要继续向上抛出，如果没有返回 <code>True</code>，那么异常将继续向上抛出，最终被 Python 解释器捕获到，继而终止程序。</p><h3 id="基于生成器实现上下文管理器"><a href="#基于生成器实现上下文管理器" class="headerlink" title="基于生成器实现上下文管理器"></a>基于生成器实现上下文管理器</h3><p>Python 还提供了另一种基于生成器来实现 <code>上下文管理器</code> 的方法。示例代码如下：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> contextlib <span class="keyword">import</span> contextmanager</span><br><span class="line"></span><br><span class="line"><span class="meta">@contextmanager</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">my_file_manager</span><span class="params">(filename, mode)</span>:</span></span><br><span class="line">    print(<span class="string">'open file'</span>)</span><br><span class="line">    f = open(filename, mode)</span><br><span class="line">    <span class="keyword">yield</span> f</span><br><span class="line">    print(<span class="string">'close file'</span>)</span><br><span class="line">    f.close()</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> my_file_manager(<span class="string">'demo.txt'</span>, <span class="string">'w'</span>) <span class="keyword">as</span> f:</span><br><span class="line">    print(<span class="string">'write file'</span>)</span><br><span class="line">    f.write(<span class="string">'demo'</span>)</span><br></pre></td></tr></table></figure><p>代码执行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">open file</span><br><span class="line">write file</span><br><span class="line">close file</span><br></pre></td></tr></table></figure><p>基于生成器的 <code>上下文管理器</code> 可以让我们不必通过类来实现，只需要一个打上 <code>@contextmanager</code> 装饰器的生成器函数即可。相比类实现的方式代码更加简洁，不过理解上来说没有类实现的方式来的清晰。但这种风格写起来更加 <code>Pythonic</code>。</p><p>其实 <code>上下文管理器</code> 的本质是能够在我们需要执行的一段代码之前和之后分别执行一段逻辑。这刚好能够利用生成器函数的特性，在 <code>yield</code> 关键字之前执行预处理工作，然后代码执行到 <code>yield</code> 处时让出函数的控制权，这时候再执行我们需要执行的一段代码逻辑，最后 <code>with</code> 语句帮我们把执行控制权交回到生成器函数内部，执行一些清理工作。</p><p>最后，一个小知识点，一个 <code>with</code> 语句实际上可以同时处理多个上下文管理器。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">with</span> open(<span class="string">'a.txt'</span>, <span class="string">'w'</span>) <span class="keyword">as</span> a, open(<span class="string">'b.txt'</span>, <span class="string">'w'</span>) <span class="keyword">as</span> b:</span><br><span class="line">    a.write(<span class="string">'hello'</span>)</span><br><span class="line">    b.write(<span class="string">'world'</span>)</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在 Web 开发中，我们经常需要对资源进行操作，如从数据库中读取数据、日志文件的写入等。这些都是常见的资源操作，而资源是有限的，所以当我们对资源操作完成以后就需要对其进行释放。如果资源没有得到释放，当资源占用数达到了操作系统的限定数，就会导致程序崩溃。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
      <category term="Pythonic" scheme="http://www.jianghushinian.cn/tags/Pythonic/"/>
    
  </entry>
  
  <entry>
    <title>Python 异常处理机制</title>
    <link href="http://www.jianghushinian.cn/2019/09/16/Python-%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86%E6%9C%BA%E5%88%B6/"/>
    <id>http://www.jianghushinian.cn/2019/09/16/Python-异常处理机制/</id>
    <published>2019-09-16T12:07:24.000Z</published>
    <updated>2019-09-16T15:11:01.813Z</updated>
    
    <content type="html"><![CDATA[<p>Python 解释器在执行代码时，如果发生错误，解释器会立即抛出异常并终止程序的运行。异常处理，实际上就是一种错误处理机制。</p><a id="more"></a><h3 id="异常的常见场景"><a href="#异常的常见场景" class="headerlink" title="异常的常见场景"></a>异常的常见场景</h3><p>程序产生异常的原因有很多种，举例来说，在 Python 中做除法运算时，<code>0</code> 是不能做除数的，否则将会抛出 <code>ZeroDivisionError</code> 异常。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="number">1</span> / <span class="number">0</span></span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">ZeroDivisionError: division by zero</span><br></pre></td></tr></table></figure><p>在 Python 控制台输入 <code>1 / 0</code> 并回车执行，解释器将立即抛出 <code>ZeroDivisionError</code> 异常，由异常提示信息<code>division by zero</code> 可知，产生异常的原因是 <code>0</code> 作为了除数。</p><p>还有很多其他容易产生异常的场景，如在发送网络请求时，有可能会遇到连接错误、超时等；访问系统资源时，有可能会遇到权限不足、资源不存在等。</p><p>在 Python 中，每一种异常都用一个类来定义，上面的 <code>ZeroDivisionError</code> 实际上就是一个异常类。Python 内置了很多种异常类，接下来就介绍下 Python 中的异常类。</p><h3 id="Python-异常类及继承关系"><a href="#Python-异常类及继承关系" class="headerlink" title="Python 异常类及继承关系"></a>Python 异常类及继承关系</h3><p>Python 提供了一个基础异常类 <code>BaseException</code>，所有其他的异常都继承于此类。以下列出了所有内置异常类及继承关系。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br></pre></td><td class="code"><pre><span class="line">BaseException</span><br><span class="line"> +-- SystemExit</span><br><span class="line"> +-- KeyboardInterrupt</span><br><span class="line"> +-- GeneratorExit</span><br><span class="line"> +-- Exception</span><br><span class="line">      +-- StopIteration</span><br><span class="line">      +-- StopAsyncIteration</span><br><span class="line">      +-- ArithmeticError</span><br><span class="line">      |    +-- FloatingPointError</span><br><span class="line">      |    +-- OverflowError</span><br><span class="line">      |    +-- ZeroDivisionError</span><br><span class="line">      +-- AssertionError</span><br><span class="line">      +-- AttributeError</span><br><span class="line">      +-- BufferError</span><br><span class="line">      +-- EOFError</span><br><span class="line">      +-- ImportError</span><br><span class="line">      |    +-- ModuleNotFoundError</span><br><span class="line">      +-- LookupError</span><br><span class="line">      |    +-- IndexError</span><br><span class="line">      |    +-- KeyError</span><br><span class="line">      +-- MemoryError</span><br><span class="line">      +-- NameError</span><br><span class="line">      |    +-- UnboundLocalError</span><br><span class="line">      +-- OSError</span><br><span class="line">      |    +-- BlockingIOError</span><br><span class="line">      |    +-- ChildProcessError</span><br><span class="line">      |    +-- ConnectionError</span><br><span class="line">      |    |    +-- BrokenPipeError</span><br><span class="line">      |    |    +-- ConnectionAbortedError</span><br><span class="line">      |    |    +-- ConnectionRefusedError</span><br><span class="line">      |    |    +-- ConnectionResetError</span><br><span class="line">      |    +-- FileExistsError</span><br><span class="line">      |    +-- FileNotFoundError</span><br><span class="line">      |    +-- InterruptedError</span><br><span class="line">      |    +-- IsADirectoryError</span><br><span class="line">      |    +-- NotADirectoryError</span><br><span class="line">      |    +-- PermissionError</span><br><span class="line">      |    +-- ProcessLookupError</span><br><span class="line">      |    +-- TimeoutError</span><br><span class="line">      +-- ReferenceError</span><br><span class="line">      +-- RuntimeError</span><br><span class="line">      |    +-- NotImplementedError</span><br><span class="line">      |    +-- RecursionError</span><br><span class="line">      +-- SyntaxError</span><br><span class="line">      |    +-- IndentationError</span><br><span class="line">      |         +-- TabError</span><br><span class="line">      +-- SystemError</span><br><span class="line">      +-- TypeError</span><br><span class="line">      +-- ValueError</span><br><span class="line">      |    +-- UnicodeError</span><br><span class="line">      |         +-- UnicodeDecodeError</span><br><span class="line">      |         +-- UnicodeEncodeError</span><br><span class="line">      |         +-- UnicodeTranslateError</span><br><span class="line">      +-- Warning</span><br><span class="line">           +-- DeprecationWarning</span><br><span class="line">           +-- PendingDeprecationWarning</span><br><span class="line">           +-- RuntimeWarning</span><br><span class="line">           +-- SyntaxWarning</span><br><span class="line">           +-- UserWarning</span><br><span class="line">           +-- FutureWarning</span><br><span class="line">           +-- ImportWarning</span><br><span class="line">           +-- UnicodeWarning</span><br><span class="line">           +-- BytesWarning</span><br><span class="line">           +-- ResourceWarning</span><br></pre></td></tr></table></figure><p>乍一看，几十种异常类感觉有点头大，其实大可不必担心。这么多异常我们可以概括的将其分为两大类，系统类异常和非系统类异常。系统类异常包括 <code>SystemExit</code>、<code>KeyboardInterrupt</code>、<code>GeneratorExit</code>，非系统类异常就是 <code>Exception</code> 及其所有子类。可以看到，只有这 4 个异常类是直接继承自异常基类 <code>BaseException</code>，所有其他异常类都继承自 <code>Exception</code>。</p><p>实际上，我们真正在编码过程中，常见的异常类并不多，如读取文件时可能抛出的 <code>FileNotFoundError</code> 异常表示文件未找到，根据下标获取列表元素时可能抛出的 <code>IndexError</code> 异常表示索引越界，以及上面示例介绍的除 0 异常 <code>ZeroDivisionError</code> 等。你并不需要把这么多种异常都背下来，你只需在遇到异常时查看文档即可。而比较常见的异常类，遇到过几次你就会了然于心了，下次写代码的时候你就能够预判哪些地方可能会出现什么样异常。</p><h3 id="处理异常"><a href="#处理异常" class="headerlink" title="处理异常"></a>处理异常</h3><p>上面介绍了 Python 中异常的继承关系，以及一些常见的异常场景。接下来我们就要学习如何处理异常，因为程序一旦产生异常，如果不进行处理，程序就会自动终止运行。做好异常处理才能够使程序在遇到异常的时候不至于终止而继续运行下去。</p><p>一个比较完整的异常处理代码示例如下：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    ...  <span class="comment"># 可能产生异常的代码</span></span><br><span class="line"><span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">    ...  <span class="comment"># 异常处理代码</span></span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    ...  <span class="comment"># 没有产生异常时执行的代码</span></span><br><span class="line"><span class="keyword">finally</span>:</span><br><span class="line">    ...  <span class="comment"># 不管是否产生异常，都会执行的代码</span></span><br></pre></td></tr></table></figure><p>其中，<code>try</code> 语句块内编写的是一些有可能产生异常的代码块，一旦产生异常，就会被 <code>except</code> 关键字所捕获，<code>except</code> 关键字后面跟着的是要捕获的异常类型，<code>Exception</code> 代表捕获所有非系统类异常，<code>as e</code> 是给捕获的异常起一个别名 <code>e</code>，这个别名可以是任意符合 Python 变量命名规范的标识符，有了这个别名，就可以在 <code>except</code> 代码块中使用这个别名了，而如果 <code>try</code> 代码块中执行的代码没有产生异常，程序就会跳过<code>except</code> 代码块直接执行 <code>else</code> 代码块中的逻辑，无论是否产生异常 <code>finally</code> 代码块中的代码最终一定会被执行。</p><p>我们还是拿前面 <code>1 / 0</code> 的示例来通过实际代码观察下异常代码执行逻辑：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    print(<span class="string">'try 代码块开始执行'</span>)</span><br><span class="line">    <span class="number">1</span> / <span class="number">0</span>  <span class="comment"># 会产生异常的代码</span></span><br><span class="line">    print(<span class="string">'try 代码块执行完毕'</span>)</span><br><span class="line"><span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">    print(<span class="string">'打印异常信息:'</span>, e)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">'没有产生异常'</span>)</span><br><span class="line"><span class="keyword">finally</span>:</span><br><span class="line">    print(<span class="string">'finally'</span>)</span><br></pre></td></tr></table></figure><p>代码执行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">try 代码块开始执行</span><br><span class="line">打印异常信息: division by zero</span><br><span class="line">finally</span><br></pre></td></tr></table></figure><p>可以看到，由于 <code>1 / 0</code> 会产生异常，所以 <code>print(&#39;try 代码块执行完毕&#39;)</code> 这句代码并没有执行，产生异常后直接执行了 <code>except</code> 代码块中的代码。由于 <code>else</code> 代码块只有在不产生异常的情况下才会被执行，所以 <code>else</code> 语句块中的代码也没有执行，而 <code>finally</code> 语句块中的代码最终被执行了。</p><p>我们再来对比下不产生异常时代码的执行逻辑：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    print(<span class="string">'try 代码块开始执行'</span>)</span><br><span class="line">    <span class="number">1</span> / <span class="number">1</span>  <span class="comment"># 不会产生异常</span></span><br><span class="line">    print(<span class="string">'try 代码块执行完毕'</span>)</span><br><span class="line"><span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">    print(<span class="string">'打印异常信息:'</span>, e)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">'没有产生异常'</span>)</span><br><span class="line"><span class="keyword">finally</span>:</span><br><span class="line">    print(<span class="string">'finally'</span>)</span><br></pre></td></tr></table></figure><p>代码执行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">try 代码块开始执行</span><br><span class="line">try 代码块执行完毕</span><br><span class="line">没有产生异常</span><br><span class="line">finally</span><br></pre></td></tr></table></figure><p>两段代码做一下对比，就很容易看出异常处理的执行逻辑了。</p><p>事实上，上面异常处理的代码示例中 <code>else</code> 和 <code>finally</code> 是可以被省略的。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    print(<span class="string">'try 代码块开始执行'</span>)</span><br><span class="line">    <span class="number">1</span> / <span class="number">0</span></span><br><span class="line">    print(<span class="string">'try 代码块执行完毕'</span>)</span><br><span class="line"><span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">    print(<span class="string">'打印异常信息:'</span>, e)</span><br></pre></td></tr></table></figure><p>这种写法也是我们编码过程中用的最多的写法。</p><p>上面说到 <code>except</code> 关键字能够捕获到异常，但其实是否执行 <code>except</code> 代码块是由跟在后面的 <code>Exception</code> 异常类决定的。<code>Exception</code> 能够捕获其所有子类及自身的异常，以上代码之所以能够捕获异常，是因为 <code>1 / 0</code> 产生的 <code>ZeroDivisionError</code> 类实际上是 <code>Exception</code> 类的子类。这里我们明确知道会产生 <code>ZeroDivisionError</code> 异常，所以更通常的写法是将 <code>except Exception as e:</code> 这句代码换成 <code>except ZeroDivisionError as e:</code>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    print(<span class="string">'try 代码块开始执行'</span>)</span><br><span class="line">    <span class="number">1</span> / <span class="number">0</span></span><br><span class="line">    print(<span class="string">'try 代码块执行完毕'</span>)</span><br><span class="line"><span class="keyword">except</span> ZeroDivisionError <span class="keyword">as</span> e:</span><br><span class="line">    print(<span class="string">'打印异常信息:'</span>, e)</span><br></pre></td></tr></table></figure><p>有时候一段代码有可能产生多种异常，如 <code>Web</code> 开发中的登录接口，往往需要前端通过 <code>AJAX</code> 将用户输入的 <code>username</code>（用户名）、<code>password</code>（密码）等信息组成 <code>JSON</code> 数据传递到后台，后台可能会先将 <code>JSON</code> 数据解析成字典，然后通过字典的 <code>key</code> 也就是 <code>username</code> 和 <code>password</code> 来获取用户输入的数据，最终完成登录操作。在这个过程中，解析 <code>JSON</code> 有可能产生异常，从解析后的字典中获取数据也有可能因为 <code>key</code> 不存在而产生异常。这个时候就需要 <code>except</code> 能够同时捕获多种异常，示例代码如下：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> json</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    data = request.body</span><br><span class="line">    json_data = json.loads(data)  <span class="comment"># 解析 JSON 有可能抛出 json.JSONDecodeError 异常</span></span><br><span class="line">    username = json_data[<span class="string">'username'</span>]  <span class="comment"># 如果 key 不存在则抛出 KeyError 异常</span></span><br><span class="line">    password = json_data[<span class="string">'password'</span>]</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">except</span> (json.JSONDecodeError, KeyError) <span class="keyword">as</span> e:</span><br><span class="line">    print(<span class="string">'打印异常信息:'</span>, e)</span><br></pre></td></tr></table></figure><p>如果想用 <code>except</code> 同时能够捕获多种异常，可以将多个异常类组成一个元组跟在 <code>except</code> 后面。这样只要抛出的异常在元组内，<code>except</code> 代码块中的代码就会被执行。</p><p>其实捕获多种异常还有另一种写法：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> json</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    data = <span class="string">'&#123;&#125;'</span></span><br><span class="line">    json_data = json.loads(data)</span><br><span class="line">    name = json_data[<span class="string">'name'</span>]</span><br><span class="line"><span class="keyword">except</span> json.JSONDecodeError <span class="keyword">as</span> e:</span><br><span class="line">    print(<span class="string">'JSONDecodeError 异常:'</span>, e)</span><br><span class="line"><span class="keyword">except</span> KeyError <span class="keyword">as</span> e:</span><br><span class="line">    print(<span class="string">'KeyError 异常:'</span>, e)</span><br><span class="line"><span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">    print(<span class="string">'其他异常:'</span>, e)</span><br></pre></td></tr></table></figure><p>这种写法的好处是，你可以在不同的捕获异常代码块中单独去写针对特定异常的处理逻辑。如果首先抛出了 <code>json.JSONDecodeError</code> 异常，那么之后的 <code>except</code> 就不会被执行了，这有点像 <code>if ... else ...</code> 的逻辑。可以看到代码的最后还加上了 <code>Exception</code> 异常类的捕获，这也是推荐的做法，因为假使程序没能如预期的抛出 <code>json.JSONDecodeError</code> 或者 <code>KeyError</code> 异常，最后还是会有一个兜底的 <code>Exception</code> 异常捕获，这样就不至于因为未知的异常而终止程序。</p><p>异常捕获还可以有更简洁的写法：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    print(<span class="string">'try 代码块开始执行'</span>)</span><br><span class="line">    <span class="number">1</span> / <span class="number">0</span></span><br><span class="line">    print(<span class="string">'try 代码块执行完毕'</span>)</span><br><span class="line"><span class="keyword">except</span>:</span><br><span class="line">    print(<span class="string">'出现了异常'</span>)</span><br></pre></td></tr></table></figure><p>这样写能够捕获任何异常，不过，通常并不建议这样做，因为在 <code>except</code> 代码块中你并不知道到底产生了哪种异常，而实际工作中，你往往需要在出现异常的地方记录日志，就需要知道是产生了哪种异常。</p><p>还有一种 <code>try ... finally</code> 的形式的写法也是可以的。通常用于不关心程序是否报错，但程序占用的资源无论如何最终都需要释放的时候。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    f = open(<span class="string">'a.txt'</span>, <span class="string">'w'</span>)</span><br><span class="line">    <span class="comment"># do something</span></span><br><span class="line"><span class="keyword">finally</span>:</span><br><span class="line">    f.close()</span><br></pre></td></tr></table></figure><p>其实我们使用的 <code>with open</code> 语句底层原理大概就是这样实现的。</p><h3 id="自定义异常"><a href="#自定义异常" class="headerlink" title="自定义异常"></a>自定义异常</h3><p>如果你观察的比较仔细，就会发现，上面的例子中出现的 <code>JSONDecodeError</code> 异常并不在文章开头所列的 Python 内置异常类中。事实上，它是 <code>json</code> 模块中定义的一个异常类。根据这个启发，你应该能猜到，其实 Python 是允许我们定义自己的异常类的。</p><p>下面就是一个自定义异常类的示例：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyException</span><span class="params">(Exception)</span>:</span></span><br><span class="line">    <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    print(<span class="number">1</span>)</span><br><span class="line">    <span class="keyword">raise</span> MyException(<span class="string">'自定义异常'</span>)</span><br><span class="line">    print(<span class="number">2</span>)</span><br><span class="line"><span class="keyword">except</span> MyException <span class="keyword">as</span> e:</span><br><span class="line">    print(e)</span><br></pre></td></tr></table></figure><p>代码执行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">1</span><br><span class="line">自定义异常</span><br></pre></td></tr></table></figure><p>自定义异常类也非常简单，我们只需要定一个类继承 <code>Exception</code> 类即可，你甚至不需要写其他任何代码。</p><p>实际上，自定义异常类可以继承任何 Python 中内置的异常类，但更推荐的继承类就是 <code>Exception</code>，虽然 <code>BaseException</code> 才是所有异常类的顶层类，并且能捕获更多的异常。但它会将 <code>KeyboardInterrupt</code> 异常捕获到，<code>KeyboardInterrupt</code> 异常通常由 <code>Ctrl + C</code> 来触发，而触发 <code>Ctrl + C</code> 时往往是我们需要主动退出程序，应该让 Python 解释器正常抛出这个异常才对。</p><h3 id="异常处理时可能会遇到的问题"><a href="#异常处理时可能会遇到的问题" class="headerlink" title="异常处理时可能会遇到的问题"></a>异常处理时可能会遇到的问题</h3><p>在异常处理过程中，你可能会遇到一些不太常见的问题。</p><p>假如在一个函数中处理异常，如果 <code>try</code> 代码块中有 <code>return</code> 语句，那么 <code>finally</code> 语句是否还会执行呢？</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">count = <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> count</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        print(<span class="string">'try'</span>)</span><br><span class="line">        <span class="keyword">return</span> count</span><br><span class="line">    <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">        print(<span class="string">'except'</span>)</span><br><span class="line">        print(e)</span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        print(<span class="string">'finally'</span>)</span><br><span class="line">        count += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">result = foo()</span><br><span class="line">print(result)</span><br><span class="line">print(count)</span><br></pre></td></tr></table></figure><p>代码执行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">try</span><br><span class="line">finally</span><br><span class="line">1</span><br><span class="line">2</span><br></pre></td></tr></table></figure><p>由代码执行结果可以看出，<code>return</code> 语句实际上是在 <code>finally</code> 语句执行之前执行的，不过，<code>return</code> 的结果是要等到执行完 <code>finally</code> 代码块内的语句之后才返回的。所以在函数外部打印的 <code>result</code> 结果为 <code>1</code>，<code>count</code> 结果最终变为了 <code>2</code>。</p><p>再看一个关于变量名和 <code>Exception</code> 别名出现同名的情况下，会产生什么结果。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">e = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">print(e)  <span class="comment"># 0</span></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    <span class="number">1</span> / <span class="number">0</span></span><br><span class="line"><span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">    print(<span class="string">'捕获异常:'</span>, e)  <span class="comment"># 捕获异常: division by zero</span></span><br><span class="line"></span><br><span class="line">print(e)  <span class="comment"># NameError: name 'e' is not defined</span></span><br></pre></td></tr></table></figure><p>代码执行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">0</span><br><span class="line">捕获异常: division by zero</span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File &quot;demo.py&quot;, line 9, in &lt;module&gt;</span><br><span class="line">    print(e)</span><br><span class="line">NameError: name &apos;e&apos; is not defined</span><br></pre></td></tr></table></figure><p>我们在最开始定义了一个变量 <code>e = 0</code>，并且在 <code>except Exception as e:</code> 时同样将 <code>Exception</code> 的别名定义成了 <code>e</code>。最终的现象是，在 <code>except</code> 代码块中可以使用这个变量，而 <code>except</code> 代码块执行完成以后，再次打印 <code>e</code> 变量就抛出了 <code>NameError</code> 异常，异常信息提示 <code>e</code> 没有定义。</p><p>实际上，出现这个问题的原因 <a href="https://docs.python.org/3/reference/compound_stmts.html#the-try-statement" target="_blank" rel="noopener">Python 官方文档</a> 已有说明。</p><p>以上处理异常的代码其实相当于：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">e = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">print(e)</span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    <span class="number">1</span> / <span class="number">0</span></span><br><span class="line"><span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        print(<span class="string">'捕获异常:'</span>, e)</span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        <span class="keyword">del</span> e</span><br><span class="line"></span><br><span class="line">print(e)</span><br></pre></td></tr></table></figure><p>在每次执行了 <code>except</code> 代码块的最后，Python 解释器都会自动删除掉异常类的别名，所以在 <code>except</code> 代码块的外面再次打印 <code>e</code> 时，抛出了 <code>NameError</code> 异常。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Python 解释器在执行代码时，如果发生错误，解释器会立即抛出异常并终止程序的运行。异常处理，实际上就是一种错误处理机制。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>Python 可变类型作为函数默认参数时的副作用</title>
    <link href="http://www.jianghushinian.cn/2019/09/15/Python-%E5%8F%AF%E5%8F%98%E7%B1%BB%E5%9E%8B%E4%BD%9C%E4%B8%BA%E5%87%BD%E6%95%B0%E9%BB%98%E8%AE%A4%E5%8F%82%E6%95%B0%E6%97%B6%E7%9A%84%E5%89%AF%E4%BD%9C%E7%94%A8/"/>
    <id>http://www.jianghushinian.cn/2019/09/15/Python-可变类型作为函数默认参数时的副作用/</id>
    <published>2019-09-15T01:55:32.000Z</published>
    <updated>2019-09-15T02:59:36.898Z</updated>
    
    <content type="html"><![CDATA[<p>在 Python 中定义函数时，可以为其指定 <code>默认参数</code>，这样就不必在每次调用函数时都传递参数进去，并且可以简化我们的代码。</p><a id="more"></a><p>在定义函数时，如果使用了 <code>可变类型</code> 来作为函数的 <code>默认参数</code>，往往会产生一些副作用。来看下面一段代码。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">(li=[])</span>:</span></span><br><span class="line">    li.append(<span class="number">1</span>)</span><br><span class="line">    print(li)</span><br><span class="line"></span><br><span class="line">foo()</span><br><span class="line">foo()</span><br><span class="line">foo()</span><br></pre></td></tr></table></figure><p>你可能想得到如下的结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[1]</span><br><span class="line">[1]</span><br><span class="line">[1]</span><br></pre></td></tr></table></figure><p>但实际上，结果却是：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[1]</span><br><span class="line">[1, 1]</span><br><span class="line">[1, 1, 1]</span><br></pre></td></tr></table></figure><p>根据结果来看，似乎每次的函数调用，<code>li</code> 列表都记录了上一次的调用结果，而并没有使用默认参数 <code>[]</code>。但是当我们在每次调用函数时，都传递一个空列表 <code>[]</code> 进去，就不会出现副作用，能够正确得到我们想要的结果。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">(li=[])</span>:</span></span><br><span class="line">    li.append(<span class="number">1</span>)</span><br><span class="line">    print(li)</span><br><span class="line"></span><br><span class="line">foo([])</span><br><span class="line">foo([])</span><br><span class="line">foo([])</span><br></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[1]</span><br><span class="line">[1]</span><br><span class="line">[1]</span><br></pre></td></tr></table></figure><p>为什么会产生这样的结果呢？其实这个问题的原因是 Python 中函数的特性所决定的。由于函数也是 <code>对象</code>，而 <code>对象</code> 可以有自己的属性，所以函数也有自己的属性。</p><p>当 <code>foo</code> 函数被创建时，它的 <code>默认参数</code> 就已经被保存在它的 <code>__defaults__</code> 属性中了。而函数只会被创建一次，以后每次执行 <code>foo()</code> 的时候，都只会调用函数，并不会去重新创建一个函数。所以函数的 <code>默认参数</code> 也只会计算一次，无论之后被调用多少次，<code>默认参数</code> 始终都是同一个 <code>对象</code>。</p><p>我们用 <code>id()</code> 函数打印出默认参数的内存地址就能够看出来。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">(li=[])</span>:</span></span><br><span class="line">    li.append(<span class="number">1</span>)</span><br><span class="line">    print(li, id(li))</span><br><span class="line"></span><br><span class="line">foo()</span><br><span class="line">foo()</span><br><span class="line">foo()</span><br></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[1] 48904632</span><br><span class="line">[1, 1] 48904632</span><br><span class="line">[1, 1, 1] 48904632</span><br></pre></td></tr></table></figure><p>可以看出，三次调用函数后，其内部打印的 <code>li</code> 地址是相同的，所以它们其实是同一个 <code>对象</code>。</p><p>知道了问题的原因，那么如果解决它呢？我们可以用 <code>None</code> 来作为函数的 <code>默认参数</code>，在调用 <code>foo</code> 函数时，在函数体内部可以通过判断 <code>li</code> 是否为 <code>None</code> 来决定是否需要使用 <code>默认值</code>。这样，当调用 <code>foo()</code> 函数并且没有传递参数时，再给 <code>li</code> 赋一个 <code>默认值</code> 即可。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">(li=None)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> li <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        li = []</span><br><span class="line">    li.append(<span class="number">1</span>)</span><br><span class="line">    print(li)</span><br><span class="line"></span><br><span class="line">foo()</span><br><span class="line">foo()</span><br><span class="line">foo()</span><br></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[1]</span><br><span class="line">[1]</span><br><span class="line">[1]</span><br></pre></td></tr></table></figure><p>这样既不用担心 <code>可变类型</code> 作为函数 <code>默认参数</code> 时的副作用，也不用在每次调用 <code>foo</code> 函数时都传递一个参数进去，能够很好的解决这个问题。</p><p>所以，当我们给函数定义 <code>默认参数</code> 时，应该尽量使用 <code>不可变类型</code> 以免产生意想不到的副作用。当然，除非你明确知道你需要用 <code>可变类型</code> 的特性来达到某些目的。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在 Python 中定义函数时，可以为其指定 &lt;code&gt;默认参数&lt;/code&gt;，这样就不必在每次调用函数时都传递参数进去，并且可以简化我们的代码。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>Python 函数参数的传递方式</title>
    <link href="http://www.jianghushinian.cn/2019/09/14/Python-%E5%87%BD%E6%95%B0%E5%8F%82%E6%95%B0%E7%9A%84%E4%BC%A0%E9%80%92%E6%96%B9%E5%BC%8F/"/>
    <id>http://www.jianghushinian.cn/2019/09/14/Python-函数参数的传递方式/</id>
    <published>2019-09-14T07:15:23.000Z</published>
    <updated>2019-09-14T13:02:32.485Z</updated>
    
    <content type="html"><![CDATA[<p>Python 中一切皆 <code>对象</code>，而对象又分为 <code>可变类型</code> 和 <code>不可变类型</code>。当将不同类型的对象作为参数传入函数时，往往会产生不同的效果。究其原因，是由于不同类型的对象有着各自不同的特性所导致的。</p><a id="more"></a><p>如果你有其他编程语言的基础，比如 <code>C</code>、<code>C++</code>、<code>JavaScript</code> 等，那么你也许听说过两种常见的函数传参方式：<code>值传递</code>（pass by value）、<code>引用传递</code>（pass by reference）。</p><p>Python 中函数参数的传递方式与大多数编程语言并不相同，既不是 <code>值传递</code>，也不是 <code>引用传递</code>。至于到底使用何种方式进行参数传递，等你阅读完本文就会明白了。不过，在此之前，我们先通过 <code>JavaScript</code> 代码来展示下到底什么是 <code>值传递</code>，什么是 <code>引用传递</code>，这样有助于我们理解 Python 中函数参数的传递方式。</p><h3 id="值传递"><a href="#值传递" class="headerlink" title="值传递"></a>值传递</h3><p>在 <code>JavaScript</code> 中，如果函数接收到的参数是原始值类型（数值、字符串、布尔值等），函数参数的传递方式即为 <code>值传递</code>。<code>值传递</code> 所表现出来的结果就是：在函数体内部修改传递进来的参数的值，并不会影响函数体外部原对象的值。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">p</span>) </span>&#123;</span><br><span class="line">    p += <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">foo(a);</span><br><span class="line"><span class="built_in">console</span>.log(a);  <span class="comment">// 1</span></span><br></pre></td></tr></table></figure><p>以上代码中，在函数体内部对变量 <code>p</code> 的值进行了修改，但是并没有影响函数外部原有变量 <code>a</code> 的值。由于是 <code>值传递</code>，所以 <code>p</code> 的值是对原有变量 <code>a</code> 的值的一份拷贝，修改 <code>p</code> 也就不会对 <code>a</code> 产生任何影响。</p><h3 id="引用传递"><a href="#引用传递" class="headerlink" title="引用传递"></a>引用传递</h3><p>如果函数接收到的参数是引用类型（数组、对象等），函数参数的传递方式即为 <code>引用传递</code>。而 <code>引用传递</code> 所表现出来的结果是：在函数体内部修改传递进来的参数的值，将会直接改变函数体外部原对象的值。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params">p</span>) </span>&#123;</span><br><span class="line">    p.push(<span class="number">4</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bar(arr);</span><br><span class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// [1, 2, 3, 4]</span></span><br></pre></td></tr></table></figure><p>在这段代码中，同样在函数体内部对变量 <code>p</code> 的值进行了修改，函数外部原有变量 <code>arr</code> 的值跟着一起被改变了。这是因为 <code>p</code> 的值是对原有变量 <code>arr</code> 的值的引用，它们俩实际上指向同一个对象 <code>[1, 2, 3]</code>。只要修改一个变量的值，另一个变量的值就会跟着改变。</p><p>值得注意的是，如果将上面的代码稍作修改，改为如下的代码。其结果表现出来的又像是 <code>值传递</code> 的特性。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params">p</span>) </span>&#123;</span><br><span class="line">    p = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>];</span><br><span class="line">    p.push(<span class="number">5</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bar(arr);</span><br><span class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// [1, 2, 3]</span></span><br></pre></td></tr></table></figure><p>实际上这个示例函数参数的传递方式依然是 <code>引用传递</code>。出现以上结果的原因是，在函数体内部，传递进来的 <code>arr</code> 的值实际上被整体替换成了另一个数组 <code>[1, 2, 3, 4]</code>，现在 <code>p</code> 所指向的对象是新的数组 <code>[1, 2, 3, 4]</code>，而不是函数体外部 <code>arr</code> 所指向的数组 <code>[1, 2, 3]</code> 了。</p><h3 id="Python-中变量与赋值"><a href="#Python-中变量与赋值" class="headerlink" title="Python 中变量与赋值"></a>Python 中变量与赋值</h3><p>搞明白了什么是 <code>值传递</code>，什么是 <code>引用传递</code>，我们回到 Python 语言中来。在正式介绍 Python 中函数参数的传递方式之前，让我们先来回顾下什么是 Python 中的 <code>变量</code>，及 <code>赋值</code> 操作。</p><p>先来看一小段示例代码。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">1</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = a + <span class="number">2</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line"><span class="number">1</span></span><br></pre></td></tr></table></figure><p>在 Python 中，<code>a = 1</code> 这句代码实际上执行效果是先在内存中创建了一个 <code>int</code> 类型的对象 <code>1</code>，然后将对象 <code>1</code> 赋值给变量 <code>a</code>。变量 <code>a</code> 就是对象 <code>1</code> 的一个标识，我们可以把它比作一个 <code>标签</code>。当执行 <code>b = a</code> 的时候，可以理解为给对象 <code>1</code> 又贴了一个新的标签 <code>b</code>。当代码执行到 <code>a = a + 2</code> 时，由于 <code>int</code> 类型对象是不可变的，即不能在原对象 <code>1</code> 上做原地加 <code>2</code> 的操作，所以相当于将对象 <code>1</code> 上的标签 <code>a</code> 撕了下来，贴在了一个新的对象 <code>3</code> 上，而对象 <code>1</code> 上原来贴的标签 <code>b</code> 依然在那里。</p><p>可以通过流程图来看下整个代码执行过程。</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/14/Python-函数参数的传递方式/a=1.png" alt="a = 1" title>                </div>                <div class="image-caption">a = 1</div>            </figure><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/14/Python-函数参数的传递方式/b=a.png" alt="b = a" title>                </div>                <div class="image-caption">b = a</div>            </figure><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/14/Python-函数参数的传递方式/a=3.png" alt="a = a + 2" title>                </div>                <div class="image-caption">a = a + 2</div>            </figure><p>以上就是 Python 中创建 <code>不可变类型</code> 对象，并将其 <code>赋值</code> 给 <code>变量</code> 的过程。而对于 <code>可变类型</code> 对象的操作会产生不同的结果，下面通过对一个 <code>list</code> 对象操作进行演示。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = []</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(<span class="number">1</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">1</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[<span class="number">1</span>]</span><br></pre></td></tr></table></figure><p>前面两行代码 <code>a = []</code> 和 <code>b = a</code> 与之前 <code>不可变类型</code> 对象的例子一样，只不过现在 <code>a</code>、<code>b</code> 两个标签都贴在了可变对象 <code>[]</code> 上。当执行 <code>a.append(1)</code> 的时候，由于 <code>list</code> 是 <code>可变类型</code>， 所以无需再次创建一个新的列表对象，而是直接在原对象上面做更改操作，因此标签 <code>a</code> 仍然贴在这个对象上，<code>a</code> 和 <code>b</code> 最终依然指向的是同一个 <code>list</code> 对象，并且这个对象由原来的 <code>[]</code> 变成了 <code>[1]</code>。</p><p>我们还是通过流程图来看下整个代码执行过程。</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/14/Python-函数参数的传递方式/a=[].png" alt="a = []" title>                </div>                <div class="image-caption">a = []</div>            </figure><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/14/Python-函数参数的传递方式/b=a[].png" alt="b = a" title>                </div>                <div class="image-caption">b = a</div>            </figure><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/14/Python-函数参数的传递方式/a.append(1).png" alt="a.append(1)" title>                </div>                <div class="image-caption">a.append(1)</div>            </figure><p>由以上两个示例可以看出，实际上在 Python 中 <code>变量</code> 本身并没有所谓的 <code>类型</code>，我们可以把它当作 <code>标签</code> 来理解，而 <code>变量</code> 所指向的 <code>对象</code> 才有 <code>类型</code> 的区分。对于 <code>不可变类型</code> 来说，改变 <code>变量</code> 的值，实际上是在对 <code>变量</code> 作重新赋值的操作；而对于 <code>可变类型</code>，改变 <code>变量</code> 的值，实际上是直接修改它所指向的 <code>对象</code> 的值。</p><h3 id="Python-中函数参数的传递方式"><a href="#Python-中函数参数的传递方式" class="headerlink" title="Python 中函数参数的传递方式"></a>Python 中函数参数的传递方式</h3><p>经过了前面的铺垫，现在可以正式介绍 Python 中函数参数的传递方式了。其实 Python 中唯一支持的函数参数的传递方式叫 <code>对象的引用传递</code>（call by object reference）。函数的 <code>形参</code> 将获得传递进来的 <code>实参</code> 的引用副本。</p><p>结合 Python 中变量与赋值就很容易理解这种传参方式了。Python 中一切皆 <code>对象</code>，而 <code>变量</code> 与 <code>对象</code> 的关系是，<code>变量</code> 会通过指向一个 <code>对象</code> 来引用这个 <code>对象</code> 的值，所以函数传参时，并不需要根据数据类型来决定使用 <code>值传递</code> 还是 <code>引用传递</code>，只需要将 <code>实参变量</code> 的引用拷贝一份给 <code>形参变量</code> 就可以了，即让 <code>形参</code> 与 <code>实参</code> 指向同一个对象。</p><p>下面分别来看两个示例，第一个示例传递给函数 <code>不可变类型</code> 参数，第二个示例传递给函数 <code>可变类型</code> 参数。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">a = <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">(p)</span>:</span></span><br><span class="line">    p += <span class="number">2</span></span><br><span class="line"></span><br><span class="line">foo(a)</span><br><span class="line">print(a)  <span class="comment"># 1</span></span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">li = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">bar</span><span class="params">(p)</span>:</span></span><br><span class="line">    p.append(<span class="number">4</span>)</span><br><span class="line"></span><br><span class="line">bar(li)</span><br><span class="line">print(li)  <span class="comment"># [1, 2, 3, 4]</span></span><br></pre></td></tr></table></figure><p>两个示例的打印结果也证实了上面我们对 Python 中函数参数传递方式的分析。实际上，拿这两段示例代码跟最开始介绍 <code>值传递</code> 和 <code>引用传递</code> 时所写的 <code>JavaScript</code> 代码进行比较，你会发现其实它们得到的竟然是同一种结果。</p><p>无论在 <code>Python</code> 中，还是在 <code>JavaScript</code> 中，对于 <code>可变类型</code> 和 <code>不可变类型</code> 当作参数传入函数时，执行完函数体内部的代码后，最终体现出来的结果是相同的。但你要明白，这两种语言对于函数参数传递方式的处理机制是完全不同的，只不过它们最终体现出来的结果相同。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Python 中一切皆 &lt;code&gt;对象&lt;/code&gt;，而对象又分为 &lt;code&gt;可变类型&lt;/code&gt; 和 &lt;code&gt;不可变类型&lt;/code&gt;。当将不同类型的对象作为参数传入函数时，往往会产生不同的效果。究其原因，是由于不同类型的对象有着各自不同的特性所导致的。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>搭建 Jupyter Notebook 服务</title>
    <link href="http://www.jianghushinian.cn/2019/09/13/%E6%90%AD%E5%BB%BA-Jupyter-Notebook-%E6%9C%8D%E5%8A%A1/"/>
    <id>http://www.jianghushinian.cn/2019/09/13/搭建-Jupyter-Notebook-服务/</id>
    <published>2019-09-13T13:27:16.000Z</published>
    <updated>2019-09-14T00:51:24.914Z</updated>
    
    <content type="html"><![CDATA[<p>工欲善其事，必先利其器。<code>Jupyter Notebook</code>  在 Python 生态中的地位想必不用我多讲，看到下图你就会明白它有多强大。</p><a id="more"></a><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/13/搭建-Jupyter-Notebook-服务/labpreview.png" alt="labpreview" title>                </div>                <div class="image-caption">labpreview</div>            </figure><p>通常我们只在自己本地的电脑上安装 <code>Jupyter Notebook</code> 来作为开发工具，但是当我们换了台新电脑或者是使用其他人的的电脑的时候，想用 <code>Jupyter Notebook</code> 来开发程序的话，就需要在新电脑上重新装一遍开发环境。如果只是临时换一台电脑的话，就会显得比较麻烦。所以，我们可以考虑在远程服务器上搭建一套 <code>Jupyter Notebook</code> 的开发环境。这样不管我们换到哪台电脑上，都可以远程连接服务器上的 <code>Jupyter Notebook</code> 环境来进行开发。</p><p>搭建 <code>Jupyter Notebook</code> 服务的目的，就是要统一开发环境，以实现在任何地方只要你有一个能够联网的 <code>浏览器</code>，就能够进行 Python 代码的开发。</p><h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><p>要搭建 <code>Jupyter Notebook</code> 服务，首先你需要有一台云服务器。我这里使用 <code>Ubuntu 18.04</code> 来作为演示。</p><p>至于 Python 的版本，我选择的是 <code>Python 3.6.7</code>。主要是因为这是我的 <code>Ubuntu 18.04</code> 云服务器自带的 Python 版本。你也可以选择其他版本，不过我不建议使用 Python2 的版本，毕竟到 <code>2020-01-01</code> 开始官方就停止维护了。</p><h3 id="安装并配置-Jupyter"><a href="#安装并配置-Jupyter" class="headerlink" title="安装并配置 Jupyter"></a>安装并配置 <code>Jupyter</code></h3><p>首先需要登录到云服务器，然后安装 <code>Jupyter</code>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ pip3 install jupyter</span><br></pre></td></tr></table></figure><p>安装好后，我们需要对其进行配置。在服务器当前登录用户的家目录下创建一个 <code>notebooks</code> 目录作为 <code>Jupyter Notebook</code> 启动后的工作目录。</p><p>（假设我当前登录的用户名为 <code>pythonic</code>，后文不再提示，你可以根据你自己的登录用户名来更改路径。）</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ mkdir /home/pythonic/notebooks</span><br></pre></td></tr></table></figure><p>在 <code>/home/pythonic/.jupyter/</code> 目录下创建名为 <code>jupyter_notebook_config.py</code> 的配置文件，用来管理 <code>Jupyter Notebook</code> 的配置。我们并不需要做额外的工作，<code>Jupyter Notebook</code> 启动时会自动找到并加载这个配置文件。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ sudo vim /home/pythonic/.jupyter/jupyter_notebook_config.py</span><br></pre></td></tr></table></figure><p>将以下内容写入到所创建的配置文件中。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">c.NotebookApp.port = 8888  # 默认启动端口，根据需要自行修改</span><br><span class="line">c.NotebookApp.ip = &apos;*&apos;  # 允许任何 IP 访问</span><br><span class="line">c.NotebookApp.notebook_dir = &apos;/home/pythonic/notebooks&apos;  # 工作目录</span><br><span class="line">c.NotebookApp.open_browser = False  # 启动时不自动打开浏览器</span><br></pre></td></tr></table></figure><p>写好配置文件，为了安全起见，我们还需要开启 <code>Jupyter Notebook</code> 的密码功能。这样不至于任何人都可以随意访问并使用。执行如下命令，根据提示输入两次密码即可。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ jupyter notebook password</span><br></pre></td></tr></table></figure><p>成功生成密码以后，<code>Jupyter Notebook</code> 会自动在 <code>/home/pythonic/.jupyter/</code> 目录下生成 <code>jupyter_notebook_config.json</code> 文件。里面存储的实际上是密码的哈希值，内容格式如下。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  &quot;NotebookApp&quot;: &#123;</span><br><span class="line">    &quot;password&quot;: &quot;sha1:b82df400d1fc:d0dc30b222bf84e8974a17206b37d3ae58b071ee&quot;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>此时，<code>Jupyter Notebook</code> 的基本配置就算完成。你可以在命令行下运行服务。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ jupyter-notebook</span><br></pre></td></tr></table></figure><p>如果顺利的话，此时打开你本地电脑的浏览器，地址栏输入远程云服务器的 <code>IP:8888</code> 就能够访问 <code>Jupyter Notebook</code> 了。</p><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/13/搭建-Jupyter-Notebook-服务/login.png" alt="login" title>                </div>                <div class="image-caption">login</div>            </figure><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/13/搭建-Jupyter-Notebook-服务/index.png" alt="index" title>                </div>                <div class="image-caption">index</div>            </figure><p>不过需要注意的是，如果你的云服务器的 <code>8888</code> 端口没有对外开放，那么本地是无法访问云服务器上的 <code>Jupyter Notebook</code> 环境的。</p><p>例如我的云服务器使用了 <code>UFW</code> 防火墙，默认 <code>8888</code> 端口是不对外开放的，可以通过命令将其开启。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo ufw allow 8888</span><br></pre></td></tr></table></figure><p>你可以根据自己的实际情况来处理可能遇到的类似问题。</p><p>现在环境已经初步搭好，不过还有一个重要的问题需要处理。我们现在通过命令行来启动 <code>Jupyter Notebook</code>，当命令行关闭，<code>Jupyter Notebook</code> 也会被同时关闭。这并不是我们想要的结果，我们需要的是无论任何时候都让 <code>Jupyter Notebook</code> 一直运行下去，这样才能在任何地点、任何时间通过浏览器来访问它。</p><h3 id="将-Jupyter-Notebook-配置为系统服务"><a href="#将-Jupyter-Notebook-配置为系统服务" class="headerlink" title="将 Jupyter Notebook 配置为系统服务"></a>将 <code>Jupyter Notebook</code> 配置为系统服务</h3><p>要解决上面的问题办法有很多，我这里选择将其配置为系统服务。这样只要不关机，<code>Jupyter Notebook</code> 就能一直运行下去。</p><p>这里涉及到 <code>Linux</code> 系统中 <code>Systemd</code> 的使用，它可以用来启动并管理 <code>守护进程</code> 。你并不需要对它有深入的了解，事实上，你只需要跟着我的步骤对其进行配置即可。如果你对其感兴趣，可以自行搜索相关资料进行学习。</p><p>先将上面运行的 <code>Jupyter Notebook</code> 程序通过按快捷键 <code>Ctrl + c</code> 停止掉，然后用如下命令来创建 <code>Jupyter Notebook</code> 系统服务的配置文件。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ sudo vim /lib/systemd/system/jupyter.service</span><br></pre></td></tr></table></figure><p>写入内容如下。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">[Unit]</span><br><span class="line"># 简短描述</span><br><span class="line">Description=Juyper-NoteBook</span><br><span class="line"></span><br><span class="line">[Service]</span><br><span class="line"># 以 fork 方式从父进程创建子进程，创建后父进程会立即退出</span><br><span class="line">Type=forking</span><br><span class="line"># 启动当前服务的命令</span><br><span class="line">ExecStart=/usr/local/bin/jupyter-notebook</span><br><span class="line"># 停止当前服务的命令</span><br><span class="line">ExecStop=/usr/bin/pkill jupyter-notebook</span><br><span class="line"># 任何原因退出后自动重启</span><br><span class="line">Restart=always</span><br><span class="line"># 重启服务之前，需要等待的秒数</span><br><span class="line">RestartSec=30s</span><br><span class="line"># 用户</span><br><span class="line">User=pythonic</span><br><span class="line"># 组</span><br><span class="line">Group=root</span><br><span class="line"></span><br><span class="line">[Install]</span><br><span class="line"># 该服务所在的 Target</span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure><p>对于配置文件，为了便于理解，我已经加入了简单的注释，你需要根据自己的情况对其进行更改。</p><p>接下来，就可以通过系统服务的形式来启动 <code>Jupyter Notebook</code> 了。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$ sudo systemctl daemon-reload</span><br><span class="line">$ sudo systemctl enable jupyter</span><br><span class="line">$ sudo systemctl start jupyter</span><br></pre></td></tr></table></figure><p>依次执行以上三条命令，<code>Jupyter Notebook</code> 就会以系统服务的形式运行起来。每当云服务器重新启动，<code>Jupyter Notebook</code> 也会跟着自动启动，并且即使因为意外情况进程挂掉也会自动重启。</p><p>如果想要停止运行 <code>Jupyter Notebook</code> 服务，只需要执行以下命令。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ sudo systemctl stop jupyter</span><br></pre></td></tr></table></figure><p>以上，就是对搭建 <code>Jupyter Notebook</code> 服务的简单介绍。如果你还需要更高级的玩法，如 <code>虚拟环境</code> 使用、配置 <code>Nginx</code> 反向代理等，可以选择自行探索，也欢迎与我一起交流探讨。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;工欲善其事，必先利其器。&lt;code&gt;Jupyter Notebook&lt;/code&gt;  在 Python 生态中的地位想必不用我多讲，看到下图你就会明白它有多强大。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
      <category term="工具" scheme="http://www.jianghushinian.cn/tags/%E5%B7%A5%E5%85%B7/"/>
    
      <category term="开发环境" scheme="http://www.jianghushinian.cn/tags/%E5%BC%80%E5%8F%91%E7%8E%AF%E5%A2%83/"/>
    
  </entry>
  
  <entry>
    <title>Python 赋值与拷贝</title>
    <link href="http://www.jianghushinian.cn/2019/09/08/Python-%E8%B5%8B%E5%80%BC%E4%B8%8E%E6%8B%B7%E8%B4%9D/"/>
    <id>http://www.jianghushinian.cn/2019/09/08/Python-赋值与拷贝/</id>
    <published>2019-09-08T10:06:03.000Z</published>
    <updated>2019-09-09T05:24:28.753Z</updated>
    
    <content type="html"><![CDATA[<h3 id="变量的赋值"><a href="#变量的赋值" class="headerlink" title="变量的赋值"></a>变量的赋值</h3><p>在 Python 中，要创建一个列表 <code>[1, 2, 3]</code> 并赋值给变量 <code>a</code> 的语法是这样的：<code>a = [1, 2, 3]</code>。通常我们称 <code>a</code> 为 <code>变量名</code>，<code>[1, 2, 3]</code> 为 <code>变量的值</code>。</p><p>给一个变量赋值的操作实际上就是将一个变量名指向一个对象，<code>a = [1, 2, 3]</code> 就相当于将变量名 <code>a</code> 指向 <code>[1, 2, 3]</code> 这个列表对象。此时将变量 <code>a</code> 再赋值给变量 <code>b</code>：<code>b = a</code>，相当于将变量 <code>b</code> 也指向 <code>[1, 2, 3]</code> 列表。最终 <code>a</code> 和 <code>b</code> 指向的是同一个 <code>[1, 2, 3]</code> 列表。</p><a id="more"></a><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(<span class="number">4</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br></pre></td></tr></table></figure><p>在 Python 中可以通过 <code>is</code> 来比较两个变量是否为同一个对象，所以 <code>a</code> 和 <code>b</code> 本质上是同一个对象，只不过是这个对象的两个 <code>名字</code> 罢了。</p><p>此时，将列表 <code>[4, 5, 6]</code> 赋值给变量 <code>a</code>：<code>a = [4, 5, 6]</code>，再次对变量 <code>a</code> 进行修改，会得到什么样的结果呢？</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(<span class="number">7</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br></pre></td></tr></table></figure><p>可以看到，将变量 <code>a</code> 重新赋值以后，再次更改 <code>a</code> 的值，<code>b</code> 的值并没有跟着变化。实际上，在执行 <code>a = [4, 5, 6]</code> 的时候，<code>a</code> 的指针变成了指向新的列表 <code>[4, 5, 6]</code>，而 <code>b</code> 的指针并没有改变，依旧指向原来的列表 <code>[1, 2, 3, 4]</code>。</p><p>我们可以用示例图来看下这个过程。</p><ol><li>Python 解释器在执行 <code>a = [1, 2, 3]</code> 时，变量 <code>a</code> 通过一个指针指向列表 <code>[1, 2, 3]</code>。</li></ol><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/08/Python-赋值与拷贝/1.png" alt="1" title>                </div>                <div class="image-caption">1</div>            </figure><ol start="2"><li>在执行 <code>b = a</code> 时，变量 <code>b</code> 也通过一个指针指向列表 <code>[1, 2, 3]</code>。</li></ol><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/08/Python-赋值与拷贝/2.png" alt="2" title>                </div>                <div class="image-caption">2</div>            </figure><ol start="3"><li>在执行 <code>a.append(4)</code> 时，实际上就是在更改变量 <code>a</code> 和变量 <code>b</code> 所指向的同一个列表对象。</li></ol><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/08/Python-赋值与拷贝/3.png" alt="3" title>                </div>                <div class="image-caption">3</div>            </figure><ol start="4"><li>将变量 <code>a</code> 重新赋值，变量 <code>b</code> 依旧指向原来的列表对象。</li></ol><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/08/Python-赋值与拷贝/4.png" alt="4" title>                </div>                <div class="image-caption">4</div>            </figure><ol start="5"><li>此时再次对变量 <code>a</code> 进行更改 <code>a.append(7)</code>，变量 <code>b</code> 的值不变。</li></ol><figure class="image-bubble">                <div class="img-lightbox">                    <div class="overlay"></div>                    <img src="//www.jianghushinian.cn/2019/09/08/Python-赋值与拷贝/5.png" alt="5" title>                </div>                <div class="image-caption">5</div>            </figure><h3 id="对象的拷贝"><a href="#对象的拷贝" class="headerlink" title="对象的拷贝"></a>对象的拷贝</h3><p>由上面的赋值操作我们可以知道，在 Python 中如果将一个变量赋值给另一个变量，那么它们最终将指向同一个对象。然而有些时候我们需要创建一个与原对象有着相同值的新对象，但是不想让它们还是同一个对象，即如果改变其中一个对象，那么另一个对象不会跟着改变。这个时候，就需要用到对象的拷贝的知识了。</p><p>对于拷贝，Python 专门提供了一个 <code>copy</code> 模块。拷贝可以分为 <code>浅拷贝</code> 和 <code>深拷贝</code>。</p><ol><li>先来看看浅拷贝</li></ol><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> copy</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = copy.copy(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a == b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">False</span></span><br></pre></td></tr></table></figure><p><code>copy.copy</code> 就是 Python 提供的浅拷贝方法。浅拷贝会在内存中重新创建一个新的对象，但对象中内部的元素还是对原对象内部元素的引用。</p><p>来看下面的例子：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> copy</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = copy.copy(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">14800096</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">14799136</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(<span class="number">11</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a[<span class="number">0</span>].append(<span class="number">22</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">22</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>], <span class="number">11</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">22</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br></pre></td></tr></table></figure><p>上面的例子中，首先创建了一个列表 <code>a</code>，其内部还嵌套了另外两个列表，然后对 <code>a</code> 进行了 <code>浅拷贝</code> 操作，得到 <code>b</code>。</p><p>执行 <code>a.append(11)</code> 操作的时候，不会对 <code>b</code> 产生影响，这是 <code>浅拷贝</code> 的性质决定的，<code>b</code> 是一个新的对象。</p><p>执行 <code>a[0].append(22)</code> 的时候，<code>b</code> 的值会跟着改变，这就是上面所说的，浅拷贝时，新对象中内部的元素还是对原对象内部元素的引用。</p><p>由此可见，<code>浅拷贝</code> 只是对嵌套对象的顶层拷贝，并不会拷贝其内部元素。因此有些时候操作还是会产生一些副作用，要想完全复制一个对象，就需要用到 <code>深拷贝</code>。</p><ol start="2"><li>深拷贝</li></ol><p>Python 提供了 <code>copy.deepcopy</code> 可以对对象进行深度拷贝。<code>深拷贝</code> 同样会创建一个新的对象，并且还会将原对象内部的元素以递归的方式进行完全拷贝。这样就实现了完全复制一个对象的目的。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> copy</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = copy.deepcopy(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">14799976</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">14800096</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(<span class="number">11</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a[<span class="number">0</span>].append(<span class="number">22</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">22</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>], <span class="number">11</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br></pre></td></tr></table></figure><p>使用 <code>深拷贝</code>，当改变变量 <code>a</code> 的值的时候，就不会对变量 <code>b</code> 的值产生影响了。这就是所谓的 <code>深拷贝</code>。</p><p>上面已经对 <code>深拷贝</code>、<code>浅拷贝</code> 进行了大致的介绍，不过还有些细节部分需要我们注意。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> copy</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = (<span class="number">1</span>, <span class="number">2</span>, <span class="number">4</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = copy.copy(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c = copy.deepcopy(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">4</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c</span><br><span class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">4</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">52975576</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">52975576</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(c)</span><br><span class="line"><span class="number">52975576</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b <span class="keyword">is</span> c</span><br><span class="line"><span class="literal">True</span></span><br></pre></td></tr></table></figure><p>出现以上现象其实是因为元组为不可变对象，因此无需重新分配内存并创建一个新的对象。所以，对于不可变对象来说，<code>浅拷贝</code>、<code>深拷贝</code> 最终的结果相同，都是指向原有的对象。但也有例外，就是当元组内部嵌套可变类型的时候。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> copy</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = ([<span class="number">1</span>, <span class="number">2</span>], <span class="number">3</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = copy.copy(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c = copy.deepcopy(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a[<span class="number">0</span>].append(<span class="number">33</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">([<span class="number">1</span>, <span class="number">2</span>, <span class="number">33</span>], <span class="number">3</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">([<span class="number">1</span>, <span class="number">2</span>, <span class="number">33</span>], <span class="number">3</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c</span><br><span class="line">([<span class="number">1</span>, <span class="number">2</span>], <span class="number">3</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">53628080</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">53628080</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(c)</span><br><span class="line"><span class="number">53628560</span></span><br></pre></td></tr></table></figure><p>上面示例的结果其实并没有脱离 <code>浅拷贝</code> 和 <code>深拷贝</code> 的特性，只不过是由于顶层对象是不可变类型，没有复制的必要，所以 <code>浅拷贝</code> 的对象和原对象还是同一个对象，原对象改变，<code>浅拷贝</code> 的对象也跟着改变。但是由于元组内部嵌套的对象是列表，列表又是可变类型，所以 <code>深拷贝</code> 就会递归的对其进行拷贝。</p><p>其实拷贝对于不可变对象来说，作用不大，真正有用的地方是在于可变对象的拷贝操作，并且只有被拷贝对象为嵌套对象的时候，才能够体现出差异。<code>浅拷贝</code> 只是对对象的顶层拷贝，<code>深拷贝</code> 是对对象的完全拷贝。</p><p>对于序列的 <code>切片</code> 操作，数据类型的构造器，实际上它们都相当于 <code>浅拷贝</code>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = a[:]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">False</span></span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = list(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">False</span></span><br></pre></td></tr></table></figure><p>对于自定义对象，还可以通过实现 <code>__copy__</code>、<code>__deepcopy__</code> 两个方法来定义 <code>浅拷贝</code> 和 <code>深拷贝</code> 的结果。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> copy</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__str__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'c'</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__copy__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'copy c'</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__deepcopy__</span><span class="params">(self, memodict=&#123;&#125;)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">'deepcopy c'</span></span><br><span class="line"></span><br><span class="line">c = C()</span><br><span class="line">print(c)</span><br><span class="line">c1 = copy.copy(c)</span><br><span class="line">print(c1)</span><br><span class="line">c2 = copy.deepcopy(c)</span><br><span class="line">print(c2)</span><br></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">c</span><br><span class="line">copy c</span><br><span class="line">deepcopy c</span><br></pre></td></tr></table></figure><p>你并不需要死记硬背来记下拷贝的特性，实际上，你只需要了解 Python 中可变类型和不可变类型的特点，就能够很容易理解了。在实际工作中，你可以根据自己的需要，使用 <code>浅拷贝</code> 或者 <code>深拷贝</code> 来解决遇到的问题。</p>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;变量的赋值&quot;&gt;&lt;a href=&quot;#变量的赋值&quot; class=&quot;headerlink&quot; title=&quot;变量的赋值&quot;&gt;&lt;/a&gt;变量的赋值&lt;/h3&gt;&lt;p&gt;在 Python 中，要创建一个列表 &lt;code&gt;[1, 2, 3]&lt;/code&gt; 并赋值给变量 &lt;code&gt;a&lt;/code&gt; 的语法是这样的：&lt;code&gt;a = [1, 2, 3]&lt;/code&gt;。通常我们称 &lt;code&gt;a&lt;/code&gt; 为 &lt;code&gt;变量名&lt;/code&gt;，&lt;code&gt;[1, 2, 3]&lt;/code&gt; 为 &lt;code&gt;变量的值&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;给一个变量赋值的操作实际上就是将一个变量名指向一个对象，&lt;code&gt;a = [1, 2, 3]&lt;/code&gt; 就相当于将变量名 &lt;code&gt;a&lt;/code&gt; 指向 &lt;code&gt;[1, 2, 3]&lt;/code&gt; 这个列表对象。此时将变量 &lt;code&gt;a&lt;/code&gt; 再赋值给变量 &lt;code&gt;b&lt;/code&gt;：&lt;code&gt;b = a&lt;/code&gt;，相当于将变量 &lt;code&gt;b&lt;/code&gt; 也指向 &lt;code&gt;[1, 2, 3]&lt;/code&gt; 列表。最终 &lt;code&gt;a&lt;/code&gt; 和 &lt;code&gt;b&lt;/code&gt; 指向的是同一个 &lt;code&gt;[1, 2, 3]&lt;/code&gt; 列表。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>JavaScript 异步操作之 Promise</title>
    <link href="http://www.jianghushinian.cn/2019/08/24/JavaScript-%E5%BC%82%E6%AD%A5%E6%93%8D%E4%BD%9C%E4%B9%8B-Promise/"/>
    <id>http://www.jianghushinian.cn/2019/08/24/JavaScript-异步操作之-Promise/</id>
    <published>2019-08-24T11:16:43.000Z</published>
    <updated>2019-08-25T09:00:10.788Z</updated>
    
    <content type="html"><![CDATA[<p>由于 JavaScript 通常是由单线程来执行代码，所以在编写 JavaScript 代码时经常需要使用异步操作来提高程序性能。一般来说异步执行在 JavaScript 中使用 <code>回调函数</code> 的形式来实现。不过近年来由于社区的推动，<code>Promise</code> 已经成为 JavaScript 异步编程的一个标准，使用 <code>Promise</code> 进行异步编程，代码的可维护性将有很大提升，尤其是使用 <code>Promise</code> 取代多层 <code>回调函数</code> 嵌套的问题。</p><a id="more"></a><h3 id="Promise-简介"><a href="#Promise-简介" class="headerlink" title="Promise 简介"></a><code>Promise</code> 简介</h3><p>下面是构造一个最简单的 <code>Promise</code> 代码示例：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 构造 Promise</span></span><br><span class="line"><span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 异步操作代码</span></span><br><span class="line">    <span class="keyword">if</span> (异步操作成功) &#123;</span><br><span class="line">        resolve(<span class="keyword">this</span>.response);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        reject(<span class="keyword">this</span>.status);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// promise 状态改变时的回调</span></span><br><span class="line">promise.then(<span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(res);  <span class="comment">// 执行成功回调</span></span><br><span class="line">&#125;, <span class="function"><span class="keyword">function</span> (<span class="params">error</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(error);  <span class="comment">// 执行失败回调</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p><code>Promise</code> 是 JavaScript 提供的一个对象，可以通过 <code>new</code> 关键字构造这个对象。<code>Promise</code> 构造函数接收一个函数作为参数，该函数接收另外两个函数作为参数，分别是 <code>resolve</code>、<code>reject</code>，这两个函数由 JavaScript 引擎自身提供，无需我们手动创建。</p><p><code>Promise</code> 对象有三种状态，分别是：<code>pending</code>(进行中)、<code>fulfilled</code>(已成功)、<code>rejected</code>(已失败)。有两种状态改变，一种是从 <code>pending</code> 变为 <code>fulfilled</code>，另一种是从 <code>pending</code> 变为 <code>rejected</code>，一旦状态改变，状态就会凝固，无法被再次更改。而 <code>resolve</code> 的作用就是将 <code>Promise</code> 对象的状态从 <code>pending</code> 变为 <code>fulfilled</code>，<code>reject</code> 的作用是将 <code>Promise</code> 对象的状态从 <code>pending</code> 变为 <code>rejected</code>。</p><p>通常我们在传递给 <code>Promise</code> 构造函数的函数顶部编写一些异步代码，例如一个 <code>AJAX</code> 请求，然后当异步代码执行成功的时候，调用 <code>resolve</code>，<code>resolve</code> 是一个函数，它能够接收参数，所以我们调用它时可以将异步代码的执行结果传递给它，此时 <code>resolve</code> 函数有两个作用，一是改变 <code>Promise</code> 的状态为 <code>fulfilled</code>，二是它能够将传递进去的参数传递到 <code>promise</code> 实例的 <code>then</code> 方法的第一个回调函数中，待后续操作使用。异步代码执行失败时调用 <code>reject</code> 函数，此时 <code>reject</code> 函数同样有两个作用，一是改变 <code>Promise</code> 的状态为 <code>rejected</code>，二是将传递进去的参数传递到 <code>promise</code> 实例的 <code>then</code> 方法的第二个回调函数中。</p><p>当 <code>Promise</code> 状态一旦改变，我们就可以通过 <code>Promise</code> 对象的实例（<code>promise</code> 变量）的 <code>then</code> 方法获取到异步操作的结果。<code>then</code> 方法接收两个回调函数作为参数，第一个回调函数会在 <code>Promise</code> 状态变为 <code>fulfilled</code> 的时候自动调用，第二个回调函数会在 <code>Promise</code> 状态变为 <code>rejected</code> 的时候自动调用。这两个回调函数分别接收一个参数，第一个回调函数接收到的 <code>res</code> 参数实际上就是我们在 <code>Promise</code> 内部异步代码执行成功时传递给 <code>resolve</code> 的参数，第二个回调函数接收到的 <code>error</code> 参数是在 <code>Promise</code> 内部异步代码执行失败时传递给 <code>reject</code> 的参数。</p><p>以上就是 <code>Promise</code> 对象的执行流程，接下来我们就用一个示例来展示 <code>Promise</code> 在实际编写代码中如何应用。</p><p>先来看一段用 <code>回调函数</code> 的写法发送 <code>AJAX</code> 请求的代码示例：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">handleResponse</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.readyState === <span class="number">4</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span>.status === <span class="number">200</span>) &#123;</span><br><span class="line">            <span class="built_in">console</span>.log(<span class="keyword">this</span>.response);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">console</span>.log(<span class="keyword">this</span>.status);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> request = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">request.open(<span class="string">'GET'</span>, <span class="string">'http://httpbin.org/get'</span>);</span><br><span class="line">request.onreadystatechange = handleResponse;</span><br><span class="line">request.send();</span><br></pre></td></tr></table></figure><p>在编写 Javascript 时这种代码非常常见，那么如何用 <code>Promise</code> 的写法来编写上面这段代码呢？请看下面的示例：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">handleResponse</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span>.readyState === <span class="number">4</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="keyword">this</span>.status === <span class="number">200</span>) &#123;</span><br><span class="line">                resolve(<span class="keyword">this</span>.response);  <span class="comment">// 请求成功时调用</span></span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                reject(<span class="keyword">this</span>.status);  <span class="comment">// 请求失败时调用</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">const</span> request = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">    request.open(<span class="string">'GET'</span>, <span class="string">'http://httpbin.org/get'</span>);</span><br><span class="line">    request.onreadystatechange = handleResponse;</span><br><span class="line">    request.send();</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">promise.then(<span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(res);  <span class="comment">// 成功回调</span></span><br><span class="line">&#125;, <span class="function"><span class="keyword">function</span> (<span class="params">error</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(error);  <span class="comment">// 失败回调</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>以上就是通过 <code>Promise</code> 来处理 <code>AJAX</code> 的写法。乍一看，觉得代码是不是变多了，也更麻烦了？事实上，的确如此，如果仅仅从发送一个简单的 <code>AJAX</code> 请求代码来看我们确实没必要采用 <code>Promise</code> 的写法，这样做只会让代码看起来更加复杂。</p><h3 id="Promise-能够解决什么问题"><a href="#Promise-能够解决什么问题" class="headerlink" title="Promise 能够解决什么问题"></a><code>Promise</code> 能够解决什么问题</h3><p>那么 <code>Promise</code> 的适用场景到底是什么，能够解决什么问题呢？</p><ol><li><code>Promise</code> 非常适合解决 <code>回调地狱</code> 问题。</li><li><code>Promise</code> 解决了回调函数中不能够使用 <code>return</code> 的问题。</li></ol><p>我们来看看 <code>Promise</code> 是如何解决这两个问题的。假设我们需要调用一个获取用户发表的博客列表的接口，而这个接口需要登录才能够调用。也就是说我们调用接口的顺序必须是：先调用登录认证接口进行登录，然后再去调用获取用户博客列表的接口。</p><p>如果使用 <code>jQuery</code> 发送 <code>AJAX</code> 请求的 <code>回调函数</code> 写法，我们有可能会写出这样的代码：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 发送第一个 AJAX 请求进行登录</span></span><br><span class="line">$.ajax(&#123;</span><br><span class="line">    type: <span class="string">'GET'</span>,</span><br><span class="line">    url: <span class="string">'http://localhost:3000/login'</span>,</span><br><span class="line">    success: <span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">        <span class="comment">// 登录成功回调函数内部，发送第二个 AJAX 请求获取数据</span></span><br><span class="line">        $.ajax(&#123;</span><br><span class="line">            type: <span class="string">'GET'</span>,</span><br><span class="line">            url: <span class="string">'http://localhost:3000/blog/list'</span>,</span><br><span class="line">            success: <span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">                <span class="built_in">console</span>.log(res);</span><br><span class="line">            &#125;,</span><br><span class="line">            error: <span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">                <span class="built_in">console</span>.log(xhr.status);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;,</span><br><span class="line">    error: <span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(xhr.status);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>可以看到，使用 <code>回调函数</code> 形式写出的代码是嵌套形式的。也就是说，每多一次请求，我们就要在 <code>success</code> 回调函数内部再写一层嵌套。如果嵌套层次过多，那么就会出现 JavaScript 令人头疼的问题 —— <code>回调地狱</code>。嵌套层次越多，代码就越难以理解，如果出现异常，那么调试将会是一个非常痛苦的过程。</p><p>我们尝试着用 <code>Promise</code> 来解决上面的问题：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> getResponse = <span class="function"><span class="keyword">function</span> (<span class="params">url</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">        $.ajax(&#123;</span><br><span class="line">            type: <span class="string">'GET'</span>,</span><br><span class="line">            url: url,</span><br><span class="line">            success: <span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">                resolve(res);</span><br><span class="line">            &#125;,</span><br><span class="line">            error: <span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">                reject(xhr);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">return</span> promise;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">getResponse(<span class="string">'http://localhost:3000/login'</span>).then(<span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">    getResponse(<span class="string">'http://localhost:3000/blog/list'</span>).then(<span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(res);</span><br><span class="line">    &#125;, <span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(xhr.status);</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;, <span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(xhr.status);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>以上是使用 <code>Promise</code> 进行重构的代码，但仔细观察你会发现：上面的代码依然在用 <code>回调</code> 的思想来写 <code>Promise</code> 代码。在第一次调用 <code>getResponse</code> 函数请求 <code>http://localhost:3000/login</code> 成功后，代码执行 <code>then</code> 方法，在 <code>then</code> 方法的内部，再一次调用了 <code>getResponse</code> 函数，这一次请求 <code>http://localhost:3000/blog/list</code> 地址，成功后执行下一个 <code>then</code> 方法，在 <code>then</code> 方法内部就能够成功获取用户博客列表的接口返回结果。</p><p>其实上面这段代码同样存在 <code>回调地狱</code> 的问题，因为如果请求次数过多，我们同样需要在一层层的嵌套函数中调用 <code>getResponse</code>。这无疑没有摆脱 <code>回调地狱</code>。</p><p>来看正确使用 <code>Promise</code> 对象解决 <code>回调地狱</code> 的示例：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> getResponse = <span class="function"><span class="keyword">function</span> (<span class="params">url</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">        $.ajax(&#123;</span><br><span class="line">            type: <span class="string">'GET'</span>,</span><br><span class="line">            url: url,</span><br><span class="line">            success: <span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">                resolve(res);</span><br><span class="line">            &#125;,</span><br><span class="line">            error: <span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">                reject(xhr.status);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">return</span> promise;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">getResponse(<span class="string">'http://localhost:3000/login'</span>).then(<span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> getResponse(<span class="string">'http://localhost:3000/blog/list'</span>);</span><br><span class="line">&#125;, <span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(xhr.status);</span><br><span class="line">&#125;).then(<span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(res);</span><br><span class="line">&#125;, <span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(xhr.status);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>在 <code>Promise</code> 接口中，<code>then</code> 方法会返回一个新的 <code>Promise</code> 实例，所以我们能够采用链式调用的写法，<code>then</code> 方法后面再接另一个 <code>then</code> 方法，理论上可以无限的接下去。</p><p>注意第一个 <code>then</code> 方法的内部，在调用 <code>getResponse(&#39;http://localhost:3000/blog/list&#39;)</code> 的前面加上了 <code>return</code>。正是这个 <code>return</code> 的存在，才使得不使用层层嵌套的写法成为可能，得以使代码展平。<code>Promise</code> 实例后面的 <code>then</code> 方法会依次执行，前一次 <code>then</code> 方法内部的 <code>return</code> 结果，会当作参数传入下一个 <code>then</code> 方法。如果使用回调函数的写法，因为其内部没有办法 <code>return</code>，所以你只能无限的增加嵌套来解决问题。</p><p>这段代码才能体现出 <code>Promise</code> 对象的真正威力，它能够把层层嵌套的代码全部展平。每多一次请求，就在后面再增加一个 <code>then</code>方法的调用，这样写出来的代码明显要直观很多，你不需要再写出多层嵌套的代码。</p><h3 id="Promise-的-then-方法和-catch-方法"><a href="#Promise-的-then-方法和-catch-方法" class="headerlink" title="Promise 的 then 方法和 catch 方法"></a><code>Promise</code> 的 <code>then</code> 方法和 <code>catch</code> 方法</h3><p>通过前面的介绍我们知道 <code>Promise</code> 的 <code>then</code> 方法接收两个回调函数作为参数。实际上，它的第二个参数并不是必填的，如果你不关心异常，那么就可以只写第一个回调函数。</p><p><code>Promise</code> 还提供了 <code>catch</code> 方法专门用来接收异常，写法如下：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">getResponse(<span class="string">'http://localhost:3000/login'</span>).then(<span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> getResponse(<span class="string">'http://localhost:3000/blog/list'</span>);</span><br><span class="line">&#125;).catch(<span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(xhr.status);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>用上面这种写法来处理异常更符合直觉，看起来也更加清晰，不用在 <code>then</code> 方法内部传入第二个回调函数去处理异常，而是单独在 <code>catch</code> 方法内部进行处理。事实上 <code>.catch(callback)</code> 方法等价于 <code>.then(undefined, callback)</code>。</p><h3 id="Promise-的-all-方法和-race-方法"><a href="#Promise-的-all-方法和-race-方法" class="headerlink" title="Promise 的 all 方法和 race 方法"></a><code>Promise</code> 的 <code>all</code> 方法和 <code>race</code> 方法</h3><p><code>Promise</code> 的 <code>all</code> 方法可以将多个 <code>Promise</code> 实例，合并为一个 <code>Promise</code> 实例。</p><p>这有什么用？假设我们需要等待多个异步操作都执行完才能执行下一步逻辑，此时就是使用 <code>all</code> 方法的绝佳时机。这种情况使用 <code>回调函数</code> 的形式不太好解决，而使用 <code>Promise</code> 就非常简单。</p><p>我们通过请求三次 <code>http://httpbin.org/get</code> 来模拟需要等待的三个异步操作，写出的代码如下：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p1 = getResponse(<span class="string">'http://httpbin.org/get'</span>);</span><br><span class="line"><span class="keyword">const</span> p2 = getResponse(<span class="string">'http://httpbin.org/get'</span>);</span><br><span class="line"><span class="keyword">const</span> p3 = getResponse(<span class="string">'http://httpbin.org/get'</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> p = <span class="built_in">Promise</span>.all([p1, p2, p3]);</span><br><span class="line">p.then(<span class="function"><span class="params">res</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(res);</span><br><span class="line">&#125;).catch(<span class="function"><span class="params">err</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(err);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p><code>all</code> 方法接收一个数组作为参数，数组每一项都是一个 <code>Promise</code> 对象，<code>Promise</code> 实例 <code>p</code> 就是合并后的对象，<code>p1</code>、<code>p2</code>、<code>p3</code> 的状态决定了 <code>p</code> 的最终状态。</p><p>只有 <code>p1</code>、<code>p2</code>、<code>p3</code> 的状态同时都变为 <code>fulfilled</code> 时，<code>p</code> 的状态才会变为 <code>fulfilled</code>。只要 <code>p1</code>、<code>p2</code>、<code>p3</code> 的状态有一个变为 <code>rejected</code>，<code>p</code> 的状态就会变为 <code>rejected</code>。当 <code>p</code> 的状态变为 <code>fulfilled</code>时，<code>p1</code>、<code>p2</code>、<code>p3</code> 的返回结果会组成一个列表，自动传递给 <code>p</code> 的 <code>then</code> 方法的回调函数。并且这个列表的顺序由传入 <code>all</code> 方法的数组中 <code>p1</code>、<code>p2</code>、<code>p3</code> 的顺序决定。</p><p>当 <code>p1</code>、<code>p2</code>、<code>p3</code> 任意一个状态变为 <code>rejected</code> 时，只有其没有自己的 <code>catch</code> 方法时才会调用 <code>all</code> 方法后面的 <code>catch</code> 方法。否则只会调用其自己的 <code>catch</code> 方法。</p><p>至于 <code>race</code> 方法的用法与 <code>all</code> 方法完全相同，其区别是：只要 <code>p1</code>、<code>p2</code>、<code>p3</code> 的状态有一个变为 <code>fulfilled</code> 时，<code>p</code> 的状态就会变为 <code>fulfilled</code>。这也是 <code>race</code> 这个单词所代表的含义 <code>竞争</code>，只要 <code>p1</code>、<code>p2</code>、<code>p3</code> 其中有一个对象率先改变了状态，那么 <code>p</code> 的状态也就随之改变并且凝固。其他两个对象内部的代码还是会执行，但其结果已经没有用了。</p><p>由于使用 <code>race</code> 的代码只需要将 <code>Promise.all([p1, p2, p3])</code> 换成 <code>Promise.race([p1, p2, p3])</code> 即可，所以这里不再给出代码示例。</p><p>以上就是关于 <code>Promise</code> 使用方法的介绍，事实上 <code>Promise</code> 不止提供了这些接口，不过其他接口很少会用到，所以这里不再过多介绍，等你熟悉了它的用法后可以再进一步去学习。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;由于 JavaScript 通常是由单线程来执行代码，所以在编写 JavaScript 代码时经常需要使用异步操作来提高程序性能。一般来说异步执行在 JavaScript 中使用 &lt;code&gt;回调函数&lt;/code&gt; 的形式来实现。不过近年来由于社区的推动，&lt;code&gt;Promise&lt;/code&gt; 已经成为 JavaScript 异步编程的一个标准，使用 &lt;code&gt;Promise&lt;/code&gt; 进行异步编程，代码的可维护性将有很大提升，尤其是使用 &lt;code&gt;Promise&lt;/code&gt; 取代多层 &lt;code&gt;回调函数&lt;/code&gt; 嵌套的问题。&lt;/p&gt;
    
    </summary>
    
      <category term="JavaScript" scheme="http://www.jianghushinian.cn/categories/JavaScript/"/>
    
    
      <category term="JavaScript" scheme="http://www.jianghushinian.cn/tags/JavaScript/"/>
    
      <category term="异步编程" scheme="http://www.jianghushinian.cn/tags/%E5%BC%82%E6%AD%A5%E7%BC%96%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>Python 中对象的比较操作 == 与 is</title>
    <link href="http://www.jianghushinian.cn/2019/08/22/Python-%E4%B8%AD%E5%AF%B9%E8%B1%A1%E7%9A%84%E6%AF%94%E8%BE%83%E6%93%8D%E4%BD%9C/"/>
    <id>http://www.jianghushinian.cn/2019/08/22/Python-中对象的比较操作/</id>
    <published>2019-08-22T13:31:26.000Z</published>
    <updated>2019-08-24T00:59:24.602Z</updated>
    
    <content type="html"><![CDATA[<p>Python 中对象的比较有两种方式 <code>==</code> 和 <code>is</code>。两种方式都能判断操作符两侧的变量值是否相等，那么它们的区别是什么呢？通过下面的介绍我们来一探究竟。</p><a id="more"></a><p>比较操作符通常用于条件语句，如下示例：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> a == b:</span><br><span class="line">    <span class="keyword">pass</span></span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> a <span class="keyword">is</span> <span class="literal">False</span>:</span><br><span class="line">    <span class="keyword">pass</span></span><br></pre></td></tr></table></figure><h3 id="与-is-的区别"><a href="#与-is-的区别" class="headerlink" title="== 与 is 的区别"></a><code>==</code> 与 <code>is</code> 的区别</h3><p><code>==</code> 操作符比较对象的值是否相等。小明有一块 <code>劳力士</code> 手表，小李也有一块同款 <code>劳力士</code> 手表，这时我们就认为这两块手表相等。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">小明的手表 = 劳力士</span><br><span class="line">小李的手表 = 劳力士</span><br><span class="line">小明的手表 == 小李的手表</span><br></pre></td></tr></table></figure><p><code>is</code> 操作符比较对象的身份标识是否相等，即对象在内存中的地址是否相同，如果两个对象的身份标识相等，就说明它们是同一个对象。小明的爸爸称呼小明叫 <code>儿子</code>，小明的女朋友称呼小明叫 <code>老公</code>，但这两个称呼都代表 <code>小明</code> 这个人，即为同一个对象。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">爸爸的儿子 = 小明</span><br><span class="line">女朋友的老公 = 小明</span><br><span class="line">爸爸的儿子 is 女朋友的老公</span><br></pre></td></tr></table></figure><p>接下来就用代码来展示一下 <code>==</code> 与 <code>is</code> 的区别：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a == b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">24603664</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">24603144</span></span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a == b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">24604144</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">24604144</span></span><br></pre></td></tr></table></figure><p>在 Python 中 <code>id</code> 函数接收一个对象作为参数，并返回该对象在内存中的地址。<br>由以上代码可以分析出：<code>==</code> 操作符只比较两个对象的值是否相等，但不比较两个对象是否为同一个对象；而 <code>is</code> 操作符并不是比较两个对象的值是否相等，而是会确认两个对象是否为同一个对象，如果为同一个对象，那么它们的值自然相等。</p><h3 id="Python-小整数对象池"><a href="#Python-小整数对象池" class="headerlink" title="Python 小整数对象池"></a>Python 小整数对象池</h3><p>以上两段代码已经能够体现出 <code>==</code> 与 <code>is</code> 的区别，不过 Python 中也有一些特殊情况，来看下面例子：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">5</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">5</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a == b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">1730274128</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">1730274128</span></span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">257</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">257</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a == b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">48558688</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">48558720</span></span><br></pre></td></tr></table></figure><p>以上代码看起来就很怪异了，同样的比较操作，只是换了一个数字结果就不同了。<br>其实出现以上结果的原因在于 Python 自身。Python 出于性能上的考虑，在解释器启动的时候就已经将 <code>-5</code> 到 <code>256</code> 的整数创建到内存中了。而当我们需要创建值在 <code>-5</code> 到 <code>256</code> 的 <code>int</code> 数字的时候，Python 并不会新开辟一块内存去创建数字，而是直接将已存在的对象返回。<br>但是如果新创建的数字不在这个范围，Python 就会为每个变量单独开辟自己的内存空间。</p><h3 id="Python-intern-机制"><a href="#Python-intern-机制" class="headerlink" title="Python intern 机制"></a>Python <code>intern</code> 机制</h3><p>再来看下面关于字符串比较的例子：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="string">'hello world'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="string">'hello world'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a == b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">49465408</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">49465448</span></span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="string">'hello'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="string">'hello'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a == b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">49429152</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">49429152</span></span><br></pre></td></tr></table></figure><p>想必根据之前数字比较的例子，你大概也能猜测到以上代码结果不同的原因了。事实上，以上结果同样是 Python 出于对性能的考虑，不过这次 Python 并没有预先将 <code>hello</code> 字符串创建到内存中，而是使用了一种叫 <code>intern</code> 的机制。<br>关于 <code>intern</code> 机制在这里我们不去深究，以后有机会专门写一篇博客来介绍。总之你需要知道在某些场景下，Python 会对字符串开启 <code>intern</code> 机制来提高性能，从而导致出现上面示例代码的结果。</p><h3 id="与-is-各自的适用场景"><a href="#与-is-各自的适用场景" class="headerlink" title="== 与 is 各自的适用场景"></a><code>==</code> 与 <code>is</code> 各自的适用场景</h3><p>什么时候用 <code>==</code>、什么时候用 <code>is</code> 呢？<br>当我们需要比较一个变量与一个 <code>单例</code> 的时候，应该使用 <code>is</code>，其他情况通常使用 <code>==</code>。<br>例如拿一个变量去跟 <code>True</code> 或 <code>False</code> 进行比较的时候就应该使用 <code>is</code>，因为用 <code>is</code> 的比较的速度要比用 <code>==</code> 更快。<br>用 <code>is</code> 比较对象的时候，只需要判断它们是否处于同一块内存地址即可，而用 <code>==</code> 比较更慢的原因在于当用 <code>==</code> 去比较对象的时候会调用对象的 <code>__eq__()</code> 方法，而 <code>__eq__()</code> 方法通常会被重载，执行其内部逻辑往往会多花一些时间。</p><p>以下就是一个重载对象 <code>__eq__()</code> 方法的例子：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyList</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, *args)</span>:</span></span><br><span class="line">        self._list = [*args]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__eq__</span><span class="params">(self, other)</span>:</span></span><br><span class="line">        result = <span class="literal">False</span></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> self._list:</span><br><span class="line">            <span class="keyword">for</span> j <span class="keyword">in</span> other._list:</span><br><span class="line">                <span class="keyword">if</span> i == j:</span><br><span class="line">                    <span class="keyword">break</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            result = <span class="literal">True</span></span><br><span class="line">        <span class="keyword">return</span> result</span><br><span class="line"></span><br><span class="line">li_1 = MyList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line">li_2 = MyList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line">print(li_1 == li_2)  <span class="comment"># True</span></span><br></pre></td></tr></table></figure><p>你可以自行尝试修改 <code>__eq__()</code> 方法内部的逻辑来观察其结果。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Python 中对象的比较有两种方式 &lt;code&gt;==&lt;/code&gt; 和 &lt;code&gt;is&lt;/code&gt;。两种方式都能判断操作符两侧的变量值是否相等，那么它们的区别是什么呢？通过下面的介绍我们来一探究竟。&lt;/p&gt;
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>Python for...else... 语句</title>
    <link href="http://www.jianghushinian.cn/2019/08/19/Python-for-else-%E8%AF%AD%E5%8F%A5/"/>
    <id>http://www.jianghushinian.cn/2019/08/19/Python-for-else-语句/</id>
    <published>2019-08-19T13:20:05.000Z</published>
    <updated>2019-08-20T01:04:01.394Z</updated>
    
    <content type="html"><![CDATA[<p>条件语句在编程语言中再寻常不过了，只要你需要编写程序，那么几乎一定会用到 <code>if</code> … <code>else</code> … 这样的条件语句进行条件判断。所以通用编程语言都会提供 <code>if</code> … <code>else</code> … 这样的语法。Python 也不例外，不过 Python 不只有 <code>if</code> … <code>else</code> … 这样的语法，它还提供了 <code>for</code> … <code>else</code> …、<code>try</code> … <code>except</code> … <code>else</code> … 等这样的语法。今天我们只讨论 <code>for</code> … <code>else</code> … 语法，来看看 Python 的 <code>else</code> 语句跟在 <code>for</code> 后面与跟在 <code>if</code> 后面有什么异同。</p><p>先看一段代码示例：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]:</span><br><span class="line">    print(i)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">'done'</span>)</span><br></pre></td></tr></table></figure><p>执行结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">done</span><br></pre></td></tr></table></figure><p>如果不执行这段代码，只看这段代码的写法，也许你会猜测它的执行结果应该只有 1、2、3 三个数，而没有 <code>done</code>。因为在 <code>if</code> … <code>else</code> … 的逻辑里，如果 <code>if</code> 的条件为真，那么就不会执行 <code>else</code> 语句下的代码块。<br>但根据打印结果来看，显然不符合我们的预期。带着疑问，我们再看下面一段示例代码：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> []:</span><br><span class="line">    print(i)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">'done'</span>)</span><br></pre></td></tr></table></figure><p>执行结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">done</span><br></pre></td></tr></table></figure><p>这次的结果只有一个 <code>done</code>，貌似这段代码看起来更合理些。因为 <code>for</code> 遍历的列表为空，所以条件不成立，那么就该执行 <code>else</code> 的逻辑，真的是这样吗？我们接着看下面示例。<br>我们知道 <code>for</code> 语句块中可以出现 <code>continue</code> 和 <code>break</code> 两个关键字，那么我们先来测试一下，如果 <code>for</code> 语句块中包含 <code>continue</code> 会是什么结果。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]:</span><br><span class="line">    <span class="keyword">if</span> i == <span class="number">2</span>:</span><br><span class="line">        <span class="keyword">continue</span></span><br><span class="line">    print(i)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">'done'</span>)</span><br></pre></td></tr></table></figure><p>执行结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">1</span><br><span class="line">3</span><br><span class="line">done</span><br></pre></td></tr></table></figure><p>这个结果和第一段代码示例差不多，只不过在 <code>i == 2</code> 的时候，跳过了本次循环，继续执行下一次循环。<br>最后我们再来看看如果 <code>for</code> 语句块中包含 <code>break</code> 会出现什么结果。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]:</span><br><span class="line">    <span class="keyword">if</span> i == <span class="number">2</span>:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">    print(i)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">'done'</span>)</span><br></pre></td></tr></table></figure><p>执行结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">1</span><br></pre></td></tr></table></figure><p>根据上面这段代码的打印结果，我想你也许已经明白了 Python 中 <code>for</code> … <code>else</code> … 语句的执行特点。没错，实际上如果按照 <code>if</code> … <code>else</code> … 是互斥的逻辑来理解 <code>for</code> … <code>else</code> …，那么其实 <code>for</code> 语句块内部的 <code>break</code> 才真正跟 <code>else</code> 是一对条件语句。<br>如果 <code>for</code> 循环中执行了 <code>break</code> 语句，那么就不会再执行 <code>else</code> 语句块内部的代码了。</p><p>知道了 Python 的 <code>for</code> … <code>else</code> … 语句的语法，但是它有什么用呢？我最开始接触 <code>for</code> … <code>else</code> … 语句的时候觉得它完全没用，以至于我写了很久的 Python 代码都没有用到过，甚至几乎已经忘记了 Python 提供的这个语法。直到我开始写 <code>ERP</code> 项目的时候，我才发现 <code>for</code> … <code>else</code> … 的真正用途。因为写 <code>ERP</code> 系统会遇到各种复杂的业务需求，如果能够用 <code>for</code> … <code>else</code> … 解决问题，代码逻辑看起来就会清晰不少，这大大增加了项目的可维护性。</p><p>假如用 <code>for</code> 循环遍历一个可迭代对象，如果可迭代对象内部有一个元素符合条件，那么就执行一个逻辑，然后 <code>break</code> 掉，如果全部都不符合，就执行 <code>else</code> 的语句去执行另一个逻辑，示例代码如下：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> 可迭代对象:</span><br><span class="line">    <span class="keyword">if</span> 条件判断:</span><br><span class="line">        执行一个逻辑</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    执行另一个逻辑</span><br></pre></td></tr></table></figure><p>这种代码显然比下面这种不用 <code>for</code> … <code>else</code> … 的写法可读性更强，并且更加符合 <code>Pythonic</code> 的写法。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flag = <span class="literal">False</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> 可迭代对象:</span><br><span class="line">    <span class="keyword">if</span> 条件判断:</span><br><span class="line">        执行一个逻辑</span><br><span class="line">        flag = <span class="literal">True</span></span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">not</span> flag:</span><br><span class="line">    执行另一个逻辑</span><br></pre></td></tr></table></figure><p>以上，就是我对 <code>for</code> … <code>else</code> … 的理解以及使用体会，希望在你遇到同样的问题时，能够对你有所帮助。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;条件语句在编程语言中再寻常不过了，只要你需要编写程序，那么几乎一定会用到 &lt;code&gt;if&lt;/code&gt; … &lt;code&gt;else&lt;/code&gt; … 这样的条件语句进行条件判断。所以通用编程语言都会提供 &lt;code&gt;if&lt;/code&gt; … &lt;code&gt;else&lt;/code&gt;
      
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
      <category term="Pythonic" scheme="http://www.jianghushinian.cn/tags/Pythonic/"/>
    
  </entry>
  
  <entry>
    <title>Python 装饰器</title>
    <link href="http://www.jianghushinian.cn/2019/08/16/Python-%E8%A3%85%E9%A5%B0%E5%99%A8/"/>
    <id>http://www.jianghushinian.cn/2019/08/16/Python-装饰器/</id>
    <published>2019-08-16T13:33:31.000Z</published>
    <updated>2019-08-22T00:52:26.900Z</updated>
    
    <content type="html"><![CDATA[<p>Python 中一切皆对象，函数也是对象。函数可以赋值给一个变量，函数可以当作参数传递个另一个函数，函数可以通过 return 语句返回函数。而装饰器就是一个能够接收函数并返回函数的函数。这话乍听起来有点绕，但装饰器本质上就是一个函数。<br>既然要学习装饰器，首先就要知道它用于什么场景，装饰器通过面向切面编程来增强代码的健壮性，比如：记录日志，处理缓存，权限校验等。接下来我们就一步一步的学习 Python 中装饰器的用法。</p><p>先来看一个简单的函数定义，函数只有一个功能，打印 Hello World：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br></pre></td></tr></table></figure><p>现在新的需求来了，要在原有的函数执行前加入日志记录功能，于是就有了下面这段代码：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'run hello'</span>)</span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br></pre></td></tr></table></figure><p>现在上面的问题解决了，只需要增加一行代码就能搞定。但问题是，实际工作场景下，我们可能需要修改的并不只是一个 <code>hello</code> 函数，有可能是 10 个、20 个函数同时需要增加日志功能。这个时候问题就来了，我们不太可能挨个函数依次复制这一行代码，况且那个时候有可能增加的不只是一行代码，可能上百行。并且这样就会造成出现大量的重复代码，当代码出现过多重复，你就要小心了，它很容易引起意想不到的 bug，并且难以排查及维护。<br>一个很容易想到的方法是定义一个专门打印日志的函数 <code>log</code>，然后在每个函数中都调用一下 <code>log</code> 函数：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">log</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'run hello'</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">()</span>:</span></span><br><span class="line">    log()</span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br></pre></td></tr></table></figure><p>这样做还是需要修 <code>hello</code> 函数内部的代码，不是说不能这样做，但这样做显然违反了 <code>开闭原则</code> 思想 —— 对已实现的功能代码封闭，对扩展开放。虽然这句话通常用在面向对象编程思想中，但函数式编程同样适用。<br>我们可以考虑用高阶函数的方式来解决这个问题，还是定义一个 <code>log</code> 函数，但这次它接收一个函数作为参数，这个函数内部先执行打印日志的功能，在 <code>log</code> 函数最后调用传递进来的函数：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">log</span><span class="params">(func)</span>:</span></span><br><span class="line">    print(<span class="string">'run hello'</span>)</span><br><span class="line">    func()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br><span class="line"></span><br><span class="line">log(hello)</span><br></pre></td></tr></table></figure><p>上面的代码就利用了函数可以当作参数传给另一个函数的特性，解决了需要修改原来函数内部代码的问题。这样做虽然功能上实现了，并且没有破坏原有函数内部的逻辑，但是却破坏了函数调用方的代码逻辑。也就是说，在原来代码中所有调用 <code>hello</code> 函数的语句不得不从 <code>hello()</code> 改为 <code>log(hello)</code>，这样做似乎更麻烦了些。</p><h3 id="简单装饰器"><a href="#简单装饰器" class="headerlink" title="简单装饰器"></a>简单装饰器</h3><p>那么，现在就是该引出 <code>装饰器</code> 这个概念的时候了，<code>装饰器</code> 非常擅长用 <code>Pythonic</code> 的方式解决这类问题。<br>来看一个最简单的装饰器的写法：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">log</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">wrapper</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">'run hello'</span>)</span><br><span class="line">        func()</span><br><span class="line">    <span class="keyword">return</span> wrapper</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br><span class="line"></span><br><span class="line">hello = log(hello)</span><br><span class="line">hello()</span><br></pre></td></tr></table></figure><p>这段代码充分体现了前面所介绍的函数的特性，函数可以赋值给一个变量，函数可以当作参数传递个另一个函数，函数可以通过 return 语句返回函数。现在的 <code>log</code> 函数就是一个 <code>装饰器</code>。<br>首先定义一个 <code>log</code> 函数，它接收一个函数作为参数，并且它的内部又定义了一个 <code>wrapper</code> 函数，<code>wrapper</code> 函数在打印日志以后，调用了传递进来的 <code>func</code> 函数（也就是<code>hello</code>函数），在 <code>log</code> 函数的最后返回这个内部定义的函数。<br>在示例代码的最底部，我们将 <code>hello</code> 函数当作参数传递给 <code>log</code> 函数，并将其返回结果又赋值给变量 <code>hello</code>，此时的 <code>hello</code> 变量所指向的其实已经不是原来的 <code>hello</code> 函数，而是 <code>log</code> 装饰器返回的内部函数 <code>wrapper</code>。<br>现在调用方无需修改调用方式，仍然使用 <code>hello()</code> 的方式去调用 <code>hello</code> 函数，但它的功能已经增强了，会自动在执行 <code>print(&#39;Hello World!&#39;)</code> 逻辑之前加上打印日志的功能。<br>上面的代码我们从功能上实现了 <code>装饰器</code> 的效果。但实际上，Python 在语法层面上直接支持了装饰器模式。仅需要一个 <code>@</code> 符号就能让上面的代码更加可读，且易于维护。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">log</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">wrapper</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">'run hello'</span>)</span><br><span class="line">        func()</span><br><span class="line">    <span class="keyword">return</span> wrapper</span><br><span class="line"></span><br><span class="line"><span class="meta">@log</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br><span class="line"></span><br><span class="line">hello()</span><br></pre></td></tr></table></figure><p><code>@</code> 符号是 Python 在语法层面上提供的语法糖，但它本质上完全等价于 <code>hello = log(hello)</code>。<br>以上就是一个最精简的符合 <code>Pythonic</code> 的 <code>装饰器</code>，无论你以后遇到多么复杂的装饰器，请记住，它最终的本质实际上就是一个函数，只不过利用了一些 Python 中的函数特性使其能够处理更复杂的业务场景。</p><h3 id="被装饰的函数带有参数、返回值的装饰器"><a href="#被装饰的函数带有参数、返回值的装饰器" class="headerlink" title="被装饰的函数带有参数、返回值的装饰器"></a>被装饰的函数带有参数、返回值的装饰器</h3><p>实际工作场景，我们写的函数往往都很复杂，想要写一个通用性更强的装饰器，还需要做一些细节部分的工作。不过你已经了解了装饰器的本质，剩下的例子理解起来并不会很费力，你只需要在特定的场景使用特定功能的装饰器就可以了。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">log</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">wrapper</span><span class="params">(*args, **kwargs)</span>:</span></span><br><span class="line">        print(<span class="string">'run hello'</span>)</span><br><span class="line">        <span class="keyword">return</span> func(*args, **kwargs)</span><br><span class="line">    <span class="keyword">return</span> wrapper</span><br><span class="line"></span><br><span class="line"><span class="meta">@log</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">(name)</span>:</span></span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">f'I am <span class="subst">&#123;name&#125;</span>.'</span></span><br><span class="line"></span><br><span class="line">result = hello(<span class="string">'xiaoming'</span>)</span><br><span class="line">print(result)</span><br></pre></td></tr></table></figure><p><code>*args, **kwargs</code> 这两个不定长参数，就很好的解决了装饰器通用性的问题，使得装饰器在装饰任何函数的时候，参数都可以原样的传入到原函数内部。<code>wrapper</code> 函数最后调用 <code>func</code> 函数的前面加上了 <code>return</code> 语句，它的作用就是将原函数的 <code>return</code> 结果返回给调用方。</p><h3 id="保持被装饰函数的元信息的装饰器"><a href="#保持被装饰函数的元信息的装饰器" class="headerlink" title="保持被装饰函数的元信息的装饰器"></a>保持被装饰函数的元信息的装饰器</h3><p><code>log</code> 装饰器内部的 <code>wrapper</code> 函数打印日志的代码 <code>print(&#39;run hello&#39;)</code> 是固定的字符串，假如我们想要让其可以根据函数名自动更改打印结果，如 <code>print(f&#39;run {函数名}.&#39;)</code> 这样的形式。<br>每个函数都有一个 <code>__name__</code> 属性，能够返回其函数名：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">(name)</span>:</span></span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br><span class="line"></span><br><span class="line">print(hello.__name__)  <span class="comment"># hello</span></span><br></pre></td></tr></table></figure><p>但问题是现在使用了 <code>log</code> 装饰器以后，原来的 <code>hello</code> 函数已经指向 <code>wrapper</code> 函数了，所以如果你测试就会发现，被装饰过的 <code>hello</code> 函数 <code>__name__</code> 属性已经变成了 <code>wrapper</code>，这显然不是我们想要的结果。<br>我们可以通过 <code>wrapper.__name__ = func.__name__</code> 一行语句解决这个问题，不过我们还有更好的办法。Python 内置了一个装饰器 <code>functools.wraps</code> 就能够帮我们解决这个问题。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> functools <span class="keyword">import</span> wraps</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">log</span><span class="params">(func)</span>:</span></span><br><span class="line"><span class="meta">    @wraps(func)</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">wrapper</span><span class="params">(*args, **kwargs)</span>:</span></span><br><span class="line">        print(<span class="string">f'run <span class="subst">&#123;func.__name__&#125;</span>'</span>)</span><br><span class="line">        <span class="keyword">return</span> func(*args, **kwargs)</span><br><span class="line">    <span class="keyword">return</span> wrapper</span><br><span class="line"></span><br><span class="line"><span class="meta">@log</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">(name)</span>:</span></span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">f'I am <span class="subst">&#123;name&#125;</span>.'</span></span><br><span class="line"></span><br><span class="line">print(hello(<span class="string">'xiaoming'</span>))</span><br><span class="line">print(hello.__name__)</span><br></pre></td></tr></table></figure><h3 id="装饰器自身带有参数"><a href="#装饰器自身带有参数" class="headerlink" title="装饰器自身带有参数"></a>装饰器自身带有参数</h3><p>也许你想控制 <code>log</code> 装饰器的日志级别，那么给装饰器传参是一个很容易想到的办法，下面来看一下需要接收参数的装饰器的例子：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> functools <span class="keyword">import</span> wraps</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">log</span><span class="params">(level)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">decorator</span><span class="params">(func)</span>:</span></span><br><span class="line"><span class="meta">        @wraps(func)</span></span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">wrapper</span><span class="params">(*args, **kwargs)</span>:</span></span><br><span class="line">            <span class="keyword">if</span> level == <span class="string">'warn'</span>:</span><br><span class="line">                print(<span class="string">f'run <span class="subst">&#123;func.__name__&#125;</span>'</span>)</span><br><span class="line">            <span class="keyword">elif</span> level == <span class="string">'info'</span>:</span><br><span class="line">                <span class="keyword">pass</span></span><br><span class="line">            <span class="keyword">return</span> func(*args, **kwargs)</span><br><span class="line">        <span class="keyword">return</span> wrapper</span><br><span class="line">    <span class="keyword">return</span> decorator</span><br><span class="line"></span><br><span class="line"><span class="meta">@log('warn')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">(name)</span>:</span></span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">f'I am <span class="subst">&#123;name&#125;</span>.'</span></span><br><span class="line"></span><br><span class="line">result = hello(<span class="string">'xiaoming'</span>)</span><br><span class="line">print(result)</span><br></pre></td></tr></table></figure><p>和之前的装饰器相比，带参数的装饰器又多了一层函数嵌套，实际上效果是这样的 <code>hello = log(&#39;warn&#39;)(hello)</code>，首先调用 <code>log(&#39;warn&#39;)</code> 返回的是内部 <code>decorator</code> 函数，接着就相当于 <code>hello = decorator(hello)</code>，实际上到这一步就和不带参数的装饰器一样了。</p><h3 id="装饰器即支持带参数又支持不带参数"><a href="#装饰器即支持带参数又支持不带参数" class="headerlink" title="装饰器即支持带参数又支持不带参数"></a>装饰器即支持带参数又支持不带参数</h3><p>有时候可能会遇到更加变态的需求，需要装饰器传不传参数都能够使用，解决方式有多种，我这里给出一个比较简单容易理解的实现。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> functools <span class="keyword">import</span> wraps</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">log</span><span class="params">(level)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> callable(level):</span><br><span class="line"><span class="meta">        @wraps(level)</span></span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">wrapper1</span><span class="params">(*args, **kwargs)</span>:</span></span><br><span class="line">            print(<span class="string">f'run <span class="subst">&#123;level.__name__&#125;</span>'</span>)</span><br><span class="line">            <span class="keyword">return</span> level(*args, **kwargs)</span><br><span class="line">        <span class="keyword">return</span> wrapper1</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">decorator</span><span class="params">(func)</span>:</span></span><br><span class="line"><span class="meta">            @wraps(func)</span></span><br><span class="line">            <span class="function"><span class="keyword">def</span> <span class="title">wrapper2</span><span class="params">(*args, **kwargs)</span>:</span></span><br><span class="line">                <span class="keyword">if</span> level == <span class="string">'warn'</span>:</span><br><span class="line">                    print(<span class="string">f'run <span class="subst">&#123;func.__name__&#125;</span>'</span>)</span><br><span class="line">                <span class="keyword">elif</span> level == <span class="string">'info'</span>:</span><br><span class="line">                    <span class="keyword">pass</span></span><br><span class="line">                <span class="keyword">return</span> func(*args, **kwargs)</span><br><span class="line">            <span class="keyword">return</span> wrapper2</span><br><span class="line">    <span class="keyword">return</span> decorator</span><br><span class="line"></span><br><span class="line"><span class="meta">@log('warn')</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">(name)</span>:</span></span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">f'I am <span class="subst">&#123;name&#125;</span>.'</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@log</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">world</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'world'</span>)</span><br><span class="line"></span><br><span class="line">print(hello(<span class="string">'xiaoming'</span>))</span><br><span class="line">world()</span><br></pre></td></tr></table></figure><p><code>callable</code> 可以判断传递进来的参数是否可调用，不过需要注意，<code>callable</code> 只支持 <code>Python3.2</code> 及以上版本，你可以查看<a href="https://docs.python.org/3/library/functions.html?highlight=callable#callable" target="_blank" rel="noopener">官方文档</a>获取详细信息。</p><h3 id="类装饰器"><a href="#类装饰器" class="headerlink" title="类装饰器"></a>类装饰器</h3><p>相比函数装饰器，类装饰更灵活，也更强大。在 Python 类中可以定义 <code>__call__</code> 方法，使其在无需实例化的情况下自身可以被调用，而此时就会执行 <code>__call__</code> 内部的代码。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Log</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, func)</span>:</span></span><br><span class="line">        self._func = func</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__call__</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">'before'</span>)</span><br><span class="line">        self._func()</span><br><span class="line">        print(<span class="string">'after'</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@Log</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'hello world!'</span>)</span><br><span class="line"></span><br><span class="line">hello()</span><br></pre></td></tr></table></figure><h3 id="装饰器装饰顺序"><a href="#装饰器装饰顺序" class="headerlink" title="装饰器装饰顺序"></a>装饰器装饰顺序</h3><p>一个函数其实可以同时被多个装饰器所装饰，那么多个装饰器的装饰顺序是怎样的呢？下面我们就来探索一下。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">a</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">wrapper</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">'a before'</span>)</span><br><span class="line">        func()</span><br><span class="line">        print(<span class="string">'a after'</span>)</span><br><span class="line">    <span class="keyword">return</span> wrapper</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">b</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">wrapper</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">'b before'</span>)</span><br><span class="line">        func()</span><br><span class="line">        print(<span class="string">'b after'</span>)</span><br><span class="line">    <span class="keyword">return</span> wrapper</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">c</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">wrapper</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">'c before'</span>)</span><br><span class="line">        func()</span><br><span class="line">        print(<span class="string">'c after'</span>)</span><br><span class="line">    <span class="keyword">return</span> wrapper</span><br><span class="line"></span><br><span class="line"><span class="meta">@a</span></span><br><span class="line"><span class="meta">@b</span></span><br><span class="line"><span class="meta">@c</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'Hello World!'</span>)</span><br><span class="line"></span><br><span class="line">hello()</span><br></pre></td></tr></table></figure><p>以上代码运行结果：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">a before</span><br><span class="line">b before</span><br><span class="line">c before</span><br><span class="line">Hello World!</span><br><span class="line">c after</span><br><span class="line">b after</span><br><span class="line">a after</span><br></pre></td></tr></table></figure><p>多装饰的语法等效于 <code>hello = a(b(c(hello)))</code>。根据打印结果不难发现这段代码的执行顺序。如果你了解过 Node.js 的 <code>Koa2</code> 框架的中间件机制，那么你一定不会陌生以上代码的执行顺序，实际上 Python 装饰器同样遵循 <code>洋葱模型</code>。多装饰器的代码执行顺序就像剥洋葱一样，先由外到内进入，然后再由内到外。<br>给大家留一个思考题：最终的 <code>hello.__name__</code> 指向哪一个装饰器内部的 <code>wrapper</code> 函数呢？</p><h3 id="装饰器实战"><a href="#装饰器实战" class="headerlink" title="装饰器实战"></a>装饰器实战</h3><p>理解了装饰器，我们就要用起来，文章开头有提到装饰器的用途，下面我们来看一个实际场景下使用装饰器的例子。<br><code>Flask</code> 是 Python Web 生态中非常流行的一个微框架，你可以到 <a href="https://github.com/pallets/flask" target="_blank" rel="noopener">GitHub</a>上查看其源码。下面就是一个用 <code>Flask</code> 编写的最小 Web 应用。<br>在这里 <code>@app.route(&quot;/&quot;)</code> 装饰器的作用就是将根路由 <code>/</code>发送过来的请求绑定到处理函数 <code>hello</code> 上面来进行处理。这样当我们启动 <code>Flask</code> Web Server 以后，在浏览器地址访问 <code>http://127.0.0.1:5000/</code> 就能够获得返回结果 <code>Hello, World!</code>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route("/")</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">hello</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Hello, World!"</span></span><br></pre></td></tr></table></figure><p>当然，更多的装饰器使用场景还是需要你自己亲自动手去探索发现。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Python 中一切皆对象，函数也是对象。函数可以赋值给一个变量，函数可以当作参数传递个另一个函数，函数可以通过 return 语句返回函数。而装饰器就是一个能够接收函数并返回函数的函数。这话乍听起来有点绕，但装饰器本质上就是一个函数。&lt;br&gt;既然要学习装饰器，首先就要知道
      
    
    </summary>
    
      <category term="Python" scheme="http://www.jianghushinian.cn/categories/Python/"/>
    
    
      <category term="Python" scheme="http://www.jianghushinian.cn/tags/Python/"/>
    
      <category term="Pythonic" scheme="http://www.jianghushinian.cn/tags/Pythonic/"/>
    
  </entry>
  
</feed>
